# Repository Changes from Upstream

This document tracks the differences between this repository and the upstream Archipelago repository.

**Upstream commit:** `fb45a2f8` (Rule Builder: Fix count resolution when Oring HasAnyCount #6048)
**Upstream date:** March 20, 2026
**Upstream version:** 0.6.7
**Last updated:** 2026-03-20

## Summary

- **Modified upstream files:** 26
- **New top-level directories:** 16
- **New files in existing directories:** 29
- **Auto-generated world directories:** 59
- **Total new files:** ~2,765
- **Total changes:** ~2,792 files

**For detailed line-by-line changes:** See the **[Diff Files README](./README.md)** for complete diff outputs of modifications to upstream files.

---

## New Top-Level Directories

The following directories are new in this repository and did not exist in the upstream commit:

### Core Project Directories
1. `CC` - Claude Code scripts and utilities
2. `docs/json` - JSON documentation system
3. `exporter` - Python rule export functionality
4. `frontend` - Web client application
5. `iframe_games` - Iframe-hosted game content
6. `scripts` - Build, test, and utility scripts
7. `test_json` - Test files for fork-specific modules
8. `apworlds` - AP world packages

### New Systems
9. `world_generator` - JSON-to-Python world generator

### Example/Demo Worlds
10. `worlds/bakingadventure` - Baking Adventure (demo world)
11. `worlds/codingadventure` - Coding Adventure (demo world)
12. `worlds/depgraph` - DepGraph: turn any DAG into a playable Archipelago world
13. `worlds/jta` - Journey to Ascension (demo world)
14. `worlds/metamath` - Metamath (demo world)
15. `worlds/toem_original` - TOEM original implementation (benchmark)
16. `worlds/toem_rule_builder` - TOEM Rule Builder version (benchmark)

### Utility Worlds
17. `worlds/json_tools_installer` - APWorld for installing JSON Tools on vanilla Archipelago

### Auto-Generated Worlds (59 worldgen + 8 worldgen2 directories)
These worlds are generated by the World Generator from JSON rules files:
- `worlds/*_worldgen/` directories (59) — first-generation worlds generated from original game rules
- `worlds/*_worldgen2/` directories (8) — second-generation worlds generated from worldgen worlds to test cross-generation compatibility
- See [World Generator documentation](../guides/world-generator.md) for details

---

## New Files in Existing Directories

These files were added to directories that already existed in the upstream commit:

### GitHub Workflows (new)
```
.github/workflows/README.md
.github/workflows/deploy-gh-pages.yml
.github/workflows/generate-presets.yml
.github/workflows/test-all-sequential.yml
.github/workflows/test-multiworld-ut-fuzz.yml
.github/workflows/test-spoiler-fuzz-single-game.yml
.github/workflows/test-spoiler-fuzz.yml
.github/workflows/test-templates.yml
.github/workflows/test-ut-fuzz-single-game.yml
.github/workflows/test-ut-fuzz.yml
.github/workflows/test-world-generator-single.yml
.github/workflows/test-world-generator.yml
.github/workflows/unittests_json.yml
```

### Project Configuration (new)
```
CLAUDE.md                            - Claude Code configuration
fuzz.py                              - Archipelago fuzzer for random YAML generation testing
jsdoc.json                           - JSDoc documentation configuration
json_tools_config.json               - JSON Tools Installer configuration
json_tools_installer_requirements.json - Version check manifest
package.json                         - Node.js dependencies and scripts
package-lock.json                    - Node.js dependency lock file
playwright.config.js                 - Playwright E2E test configuration
vitest.config.js                     - Vitest unit test configuration
```

### Rule Builder (new files)
```
rule_builder/README.md
rule_builder/_ast_utils.py
rule_builder/ast_explain.py
rule_builder/ast_format.py
rule_builder/pathfinding.py
```

### Test Files (new)
```
test/general/test_schema_validation.py
```

### World Utilities (new)
```
worlds/RomlessUtils.py
```

---

## Modified Files

The following 26 files have been modified from the upstream version:

### Core Files (see diff-files/core-files.diff)
```
settings.py
```

### Configuration Files (see diff-files/config-files.diff)
```
.gitattributes
.github/workflows/codeql-analysis.yml
.gitignore
pytest.ini
README.md
```

### Rule Builder (see [fork-vs-upstream-rule-builder.md](./rule-builder/fork-vs-upstream-rule-builder.md) and [rule-builder-modifications.md](./rule-builder/rule-builder-modifications.md))
```
rule_builder/__init__.py
rule_builder/rules.py
```

Note: `rule_builder/` exists in upstream (merged from PR #5048). The fork extends it with AST format support, additional rule types, pathfinding tools, and JSON serialization. `rule_builder/cached_world.py` and `rule_builder/options.py` are **unmodified** from upstream.

### World Bug/Determinism Fixes (see diff-files/world-minor-fixes.diff, diff-files/alttp-bunny-rules.diff)
```
worlds/alttp/Rules.py
worlds/landstalker/Hints.py
worlds/lufia2ac/Options.py
worlds/shapez/__init__.py
```

### Test Files (modified)
```
test/general/test_implemented.py
test/general/test_items.py
test/general/test_reachability.py
test/general/test_rule_builder.py
```

### World Init Files (see diff-files/world-init-files.diff)
```
worlds/alttp/__init__.py
worlds/ff1/__init__.py
worlds/lufia2ac/__init__.py
worlds/mmbn3/__init__.py
worlds/oot/__init__.py
worlds/smw/__init__.py
worlds/soe/__init__.py
worlds/tloz/__init__.py
worlds/yoshisisland/__init__.py
```

---

## Key Modifications

### Core Files
- **settings.py** - `skip_required_files` global, `Group.__getattribute__` bypass for missing ROM paths, and early extraction from host.yaml `json_tools` section. Required for romless world patches.

Note: `BaseClasses.py`, `Main.py`, `Utils.py`, `CommonClient.py`, and `Launcher.py` now match upstream exactly. JSON export and sphere logging are handled entirely by monkey patches (installed at runtime by the JSON Tools Installer).

### World Implementations (skip_required_files support)
Modified world implementations to support generation without ROM files:
- A Link to the Past (alttp)
- Final Fantasy I (ff1)
- Lufia II Ancient Cave (lufia2ac)
- Mega Man Battle Network 3 (mmbn3)
- Ocarina of Time (oot)
- Super Mario World (smw)
- Secret of Evermore (soe)
- The Legend of Zelda (tloz)
- Yoshi's Island (yoshisisland)

### Test Files
- **test/general/test_implemented.py** - Added "The Messenger" and "Overcooked! 2" to `excluded_games` in `test_slot_data` to prevent flaky fill failures. These original worlds have tight access rules that cause `FillError` with certain random seeds when the fill algorithm's item ordering creates an unresolvable deadlock.
- **test/general/test_items.py** - Added `DLCQuest` coins to item exclusion dict; added logic to propagate exclusions from base games to WorldGen variants (e.g., "A Link to the Past WorldGen" inherits "A Link to the Past" exclusions)
- **test/general/test_reachability.py** - Added `shapez` "Achievements needing a MAM" to unreachable regions; added same WorldGen variant propagation logic
- **test/general/test_rule_builder.py** - Added ~600 lines of evaluation tests for fork-only Rule Builder rule types: CountItem, CountFromList, Compare, Arithmetic, MinValue, MaxValue, WeightedSum, and more

As of the 2026-06-25 overlay re-base, the fork's `rule_builder/` is the **clean upstream base plus separate overlay modules**, not a monolithic `rules.py`. See [fork-vs-upstream-rule-builder.md](./rule-builder/fork-vs-upstream-rule-builder.md) for the detailed comparison.
- **rule_builder/rules.py** - Upstream base with minimal additive edits (`get_value`/`get_count`/`to_dict`/`__lshift__` base methods, widened `Has.count`, `_make_hashable` for HelperCall args). Otherwise kept byte-identical to upstream so future merges stay low-conflict.
- **rule_builder/extra_rules.py** - Fork overlay: the 15 fork-specific rule types (Arithmetic, ASTRule, CountItem, CountFromList, Compare, etc.), self-registered into `DEFAULT_RULES`.
- **rule_builder/world_mixin.py** - Fork overlay: `RuleWorldMixin` / `RuleBuilderLogicMixin` (World bound to object at runtime, LogicMixin resolved lazily).
- **rule_builder/{ast_format,ast_explain,_ast_utils,pathfinding}.py** - Fork overlay: AST-format support and pathfinding helpers.
- **rule_builder/__init__.py** - Upstream: empty file. Fork: re-exports the full API surface (57 names) across the split modules plus the upstream field_resolvers.

### Configuration Files
- **.gitignore** - Added patterns for project-specific files
- **.gitattributes** - Merge strategy for .gitignore and README.md
- **pytest.ini** - Added `test_json` to testpaths for fork-specific test modules
- **README.md** - Documentation updates

Note: `requirements.txt` matches upstream exactly. Fork-specific dependencies (dill, astunparse, psutil) are handled by the JSON Tools Installer.

### GitHub Workflows
- **codeql-analysis.yml** - Added explicit permissions for security events (safer for forks)
- **unittests.yml** - Disabled (renamed to .disabled)
- Multiple new workflows for testing and deployment

---

## New Major Features

### Rule Builder Extensions (`rule_builder/`)
The upstream Rule Builder ([PR #5048](https://github.com/ArchipelagoMW/Archipelago/pull/5048), now merged) is extended with:
- AST format parsing (`ast_format.py`) for converting JSON AST rules to Rule Builder objects
- Human-readable explanation system (`ast_explain.py`)
- Pathfinding tools (`pathfinding.py`) for region accessibility analysis
- Additional rule types for full AST format coverage (15 new classes)
- JSON serialization/deserialization (`to_dict()`/`from_dict()` on Resolved rules)

The upstream files `rule_builder/cached_world.py` and `rule_builder/options.py` are unmodified. See [rule-builder-modifications.md](./rule-builder/rule-builder-modifications.md) and [fork-vs-upstream-rule-builder.md](./rule-builder/fork-vs-upstream-rule-builder.md) for details.

### World Generator (`world_generator/`)
Converts JSON rules files back into functional Archipelago world packages:
- Generates complete Python world structure
- Converts CC-format rules to Rule Builder syntax
- Supports canonical placement mode for testing

### Skip Required Files Mode
Allows world generation without ROM files for JSON export purposes. Enables:
- Export of game rules without owning ROMs
- CI/CD testing without proprietary files
- Development and debugging workflows

### Pickle Export Mode (`exporter/pickle_exporter.py`)
An alternative to JSON export that pickles the multiworld directly using dill:
- Preserves exact lambdas and functions in access rules
- Faster tracker loading (no world regeneration needed)
- Gzip-compressed for smaller file sizes
- Supports per-game tracking mode configuration

### Tracking Mode Configuration (`scripts/data/tracking-mode-config.json`)
Per-game configuration for Universal Tracker modes:
- Specifies which modes (worldgen, pickle, original) pass for each game
- Generated from automated UT fuzz test results
- Supports both bundled worlds and APWorlds

---

## External Components

This repository includes several components based on or taken from other community projects:

### Modified from External Sources

| Component | Location | Source | Status |
|-----------|----------|--------|--------|
| **Rule Builder** | `rule_builder/` | [PR #5048](https://github.com/ArchipelagoMW/Archipelago/pull/5048) by drtchops | Modified - extended with AST formatting, explanation system, and JSON serialization |
| **Universal Tracker** | `worlds/tracker/` | [FarisTheAncient/Archipelago](https://github.com/FarisTheAncient/Archipelago) | Modified - integrated with JSON export system |
| **Fuzzer** | `fuzz.py` | [Archipelago-fuzzer](https://github.com/Eijebong/Archipelago-fuzzer) by Eijebong | Modified - adapted for this project's testing needs |

### Unmodified from External Sources

| Component | Location | Source | Notes |
|-----------|----------|--------|-------|
| **APWorld Manager** | `worlds/apworld_manager/` | [silasary/Archipelago](https://github.com/silasary/Archipelago) | Unmodified - provides APWorld installation GUI |
| **APWorld Index** | (referenced by APWorld Manager) | [silasary/apworlds](https://github.com/silasary/apworlds) | External repository for APWorld listings |

### Original to This Project

The following major components were developed specifically for this project:
- `exporter/` - JSON rule export system and pickle exporter
- `world_generator/` - JSON-to-Python world generator
- `frontend/` - Web client application
- `worlds/json_tools_installer/` - APWorld installer for JSON Tools
- `scripts/` - Build, test, and utility scripts

---

## Notes

- This repository extends the upstream Archipelago with custom functionality including:
  - JSON export capabilities for game logic
  - Modular web client application
  - Rule Builder declarative rule system
  - World Generator for JSON-to-Python conversion
  - Claude Code integration scripts
  - Comprehensive automated testing infrastructure
  - 59 auto-generated worldgen packages + 8 second-generation worldgen2 packages from JSON rules

- The latest sync from upstream was performed on March 20, 2026, bringing the fork up to Archipelago 0.6.7 (commit `fb45a2f8`)

- Most changes are additions rather than modifications, with only 26 files modified from upstream

- The majority of the ~2,000 new files are contained within:
  - `frontend/` - Web client (~400 files)
  - `worlds/*_worldgen/` - Auto-generated worlds (~550 files)
  - `exporter/` - Export tools (~50 files)
  - `scripts/` - Build and test scripts (~100 files)

---

**Last updated:** 2026-03-20
**Base commit:** fb45a2f8 (Rule Builder: Fix count resolution when Oring HasAnyCount #6048)
**Upstream version:** 0.6.7
