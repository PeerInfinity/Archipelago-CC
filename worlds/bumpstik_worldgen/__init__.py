"""
Bumper Stickers WorldGen world implementation for Archipelago.

Auto-generated by world_generator.
"""
import json
import os

from typing import ClassVar, Dict, List, Set, Any, TYPE_CHECKING
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

if TYPE_CHECKING:
    from BaseClasses import CollectionState, MultiWorld

from .Items import item_table, ItemData, BumperStickersWorldGenItem
from .Locations import location_table, BumperStickersWorldGenLocation
from .Options import BumperStickersWorldGenOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "Booster Bumper": 5,
    "Hazard Bumper": 25,
    "Rainbow Trap": 3,
    "Score Bonus": 22,
    "Spinner Trap": 2,
    "Starting Paint Can": 3,
    "Starting Turner": 3,
    "Task Advance": 4,
    "Treasure Bumper": 33,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {

}

# Starting items - items the player begins with (precollected)
STARTING_ITEMS: Dict[str, int] = {

}


class BumperStickersWorldGenWeb(WebWorld):
    """Web interface for Bumper Stickers WorldGen."""
    theme = "stone"
    game_info_languages: List[str] = ['en']
    tutorials = [
        Tutorial(
            "Bumper Stickers Setup Guide",
            "A guide to setting up the Archipelago Bumper Stickers software on your computer.",
            "English",
            "setup_en.md",
            "setup/en",
            ["KewlioMZX"]
        )
    ]


class BumpStikWorld(RuleWorldMixin, World):
    """
    Bumper Stickers is a match-three puzzle game unlike any you've seen.
    Launch Bumpers onto the field, and match them in sets of three of the same color.
    How long can you go without getting Jammed?
    """

    game: ClassVar[str] = "Bumper Stickers WorldGen"
    web: ClassVar[WebWorld] = BumperStickersWorldGenWeb()

    options_dataclass = BumperStickersWorldGenOptions
    options: BumperStickersWorldGenOptions

    # Disable rule caching - requires CollectionState.rule_builder_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    # Expose item_table as item_name_to_item for exporter compatibility
    # This allows the exporter handler to find item classifications
    item_name_to_item: ClassVar[Dict[str, "ItemData"]] = item_table

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    item_name_groups: ClassVar[Dict[str, frozenset]] = {
        "Everything": frozenset(["Nothing", "Score Bonus", "Task Advance", "Starting Turner", "Reserved", "Starting Paint Can", "Booster Bumper", "Hazard Bumper", "Treasure Bumper", "Rainbow Trap", "Spinner Trap", "Killer Trap"]),
        "Helpers": frozenset(["Task Advance", "Starting Turner", "Starting Paint Can"]),
        "Targets": frozenset(["Booster Bumper", "Hazard Bumper", "Treasure Bumper"]),
        "Traps": frozenset(["Rainbow Trap", "Spinner Trap", "Killer Trap"]),
    }

    # Placements are deterministically reproduced by world generator
    is_canonical: ClassVar[bool] = True

    # Canonical item placements - where items belong in the "vanilla" game
    # Used by exporter to distinguish canonical placements from always-locked items
    canonical_placements: ClassVar[Dict[str, str]] = {
        "Bonus Booster 5": "Treasure Bumper",
        "Level 1 - 1000 Level Points": "Treasure Bumper",
        "Level 1 - 1000 Points": "Treasure Bumper",
        "Level 1 - 2000 Level Points": "Treasure Bumper",
        "Level 1 - 250 Points": "Treasure Bumper",
        "Level 1 - Combo 5": "Treasure Bumper",
        "Level 2 - 100 Level Bumpers": "Treasure Bumper",
        "Level 2 - 1000 Level Points": "Treasure Bumper",
        "Level 2 - 1500 Points": "Treasure Bumper",
        "Level 2 - 2000 Level Points": "Treasure Bumper",
        "Level 2 - 2000 Points": "Treasure Bumper",
        "Level 2 - 25 Level Bumpers": "Treasure Bumper",
        "Level 3 - 100 Level Bumpers": "Treasure Bumper",
        "Level 3 - 1600 Points": "Treasure Bumper",
        "Level 3 - 2400 Points": "Treasure Bumper",
        "Level 3 - 3200 Points": "Treasure Bumper",
        "Level 3 - 50 Level Bumpers": "Treasure Bumper",
        "Level 3 - 75 Level Bumpers": "Treasure Bumper",
        "Level 4 - 1500 Points": "Treasure Bumper",
        "Level 4 - 9000 Level Points": "Treasure Bumper",
        "Level 4 - Combo 5": "Treasure Bumper",
        "Level 5 - 50,000+ Total Points": "Treasure Bumper",
        "Treasure Bumper 1": "Treasure Bumper",
        "Treasure Bumper 13": "Treasure Bumper",
        "Treasure Bumper 17": "Treasure Bumper",
        "Treasure Bumper 2": "Treasure Bumper",
        "Treasure Bumper 25": "Treasure Bumper",
        "Treasure Bumper 26": "Treasure Bumper",
        "Treasure Bumper 28": "Treasure Bumper",
        "Treasure Bumper 3": "Treasure Bumper",
        "Treasure Bumper 31": "Treasure Bumper",
        "Treasure Bumper 4": "Treasure Bumper",
        "Treasure Bumper 9": "Treasure Bumper",
        "Bonus Booster 1": "Score Bonus",
        "Bonus Booster 2": "Score Bonus",
        "Bonus Booster 3": "Score Bonus",
        "Bonus Booster 4": "Score Bonus",
        "Level 1 - 500 Level Points": "Score Bonus",
        "Level 1 - 500 Points": "Score Bonus",
        "Level 2 - 50 Level Bumpers": "Score Bonus",
        "Level 2 - 500 Points": "Score Bonus",
        "Level 2 - Chain x2": "Score Bonus",
        "Level 3 - 2000 Level Points": "Score Bonus",
        "Level 3 - 6000 Level Points": "Score Bonus",
        "Level 4 - 12000 Level Points": "Score Bonus",
        "Level 4 - 3000 Level Points": "Score Bonus",
        "Level 4 - 3000 Points": "Score Bonus",
        "Level 4 - 75 Level Bumpers": "Score Bonus",
        "Treasure Bumper 10": "Score Bonus",
        "Treasure Bumper 16": "Score Bonus",
        "Treasure Bumper 20": "Score Bonus",
        "Treasure Bumper 23": "Score Bonus",
        "Treasure Bumper 30": "Score Bonus",
        "Treasure Bumper 5": "Score Bonus",
        "Treasure Bumper 7": "Score Bonus",
        "Level 1 - 75 Level Bumpers": "Booster Bumper",
        "Level 1 - 750 Points": "Booster Bumper",
        "Treasure Bumper 14": "Booster Bumper",
        "Treasure Bumper 22": "Booster Bumper",
        "Treasure Bumper 6": "Booster Bumper",
        "Level 1 - 1500 Level Points": "Hazard Bumper",
        "Level 1 - 25 Level Bumpers": "Hazard Bumper",
        "Level 1 - 50 Level Bumpers": "Hazard Bumper",
        "Level 2 - 1000 Points": "Hazard Bumper",
        "Level 2 - 3000 Level Points": "Hazard Bumper",
        "Level 2 - 4000 Level Points": "Hazard Bumper",
        "Level 2 - Combo 5": "Hazard Bumper",
        "Level 3 - 125 Level Bumpers": "Hazard Bumper",
        "Level 3 - 25 Level Bumpers": "Hazard Bumper",
        "Level 3 - 4000 Level Points": "Hazard Bumper",
        "Level 3 - 800 Points": "Hazard Bumper",
        "Level 3 - 8000 Level Points": "Hazard Bumper",
        "Level 3 - Chain x2": "Hazard Bumper",
        "Level 3 - Combo 5": "Hazard Bumper",
        "Level 3 - Combo 7": "Hazard Bumper",
        "Level 4 - 125 Level Bumpers": "Hazard Bumper",
        "Level 4 - 150 Level Bumpers": "Hazard Bumper",
        "Level 4 - 25 Level Bumpers": "Hazard Bumper",
        "Level 4 - 6000 Points": "Hazard Bumper",
        "Level 4 - Combo 7": "Hazard Bumper",
        "Treasure Bumper 15": "Hazard Bumper",
        "Treasure Bumper 21": "Hazard Bumper",
        "Treasure Bumper 27": "Hazard Bumper",
        "Treasure Bumper 29": "Hazard Bumper",
        "Treasure Bumper 32": "Hazard Bumper",
        "Treasure Bumper 18": "Spinner Trap",
        "Treasure Bumper 8": "Spinner Trap",
        "Level 2 - 75 Level Bumpers": "Starting Paint Can",
        "Level 4 - 4500 Points": "Starting Paint Can",
        "Treasure Bumper 11": "Starting Paint Can",
        "Level 4 - 6000 Level Points": "Task Advance",
        "Level 5 - Cleared all Hazards": "Task Advance",
        "Treasure Bumper 12": "Task Advance",
        "Treasure Bumper 24": "Task Advance",
        "Level 3 - All Clear, 3 colors": "Starting Turner",
        "Level 4 - Chain x3": "Starting Turner",
        "Treasure Bumper 19": "Starting Turner",
        "Level 4 - 100 Level Bumpers": "Rainbow Trap",
        "Level 4 - 50 Level Bumpers": "Rainbow Trap",
        "Level 4 - Chain x2": "Rainbow Trap",
    }

    # Canonical placement advancement status - for items with mixed classifications
    # True = progression, False = useful/filler. Used to select correct item copy during placement.
    canonical_placement_advancements: ClassVar[Dict[str, bool]] = {
        "Level 1 - 250 Points": True,
        "Level 1 - 500 Points": False,
        "Level 1 - 750 Points": True,
        "Level 1 - 1000 Points": True,
        "Level 1 - 500 Level Points": False,
        "Level 1 - 1000 Level Points": True,
        "Level 1 - 1500 Level Points": True,
        "Level 1 - 2000 Level Points": True,
        "Level 1 - 25 Level Bumpers": True,
        "Level 1 - 50 Level Bumpers": True,
        "Level 1 - 75 Level Bumpers": True,
        "Level 1 - Combo 5": True,
        "Bonus Booster 1": False,
        "Treasure Bumper 1": True,
        "Treasure Bumper 2": True,
        "Treasure Bumper 3": True,
        "Treasure Bumper 4": True,
        "Treasure Bumper 5": False,
        "Treasure Bumper 6": True,
        "Treasure Bumper 7": False,
        "Treasure Bumper 8": False,
        "Level 2 - 500 Points": False,
        "Level 2 - 1000 Points": True,
        "Level 2 - 1500 Points": True,
        "Level 2 - 2000 Points": True,
        "Level 2 - 1000 Level Points": True,
        "Level 2 - 2000 Level Points": True,
        "Level 2 - 3000 Level Points": True,
        "Level 2 - 4000 Level Points": True,
        "Level 2 - 25 Level Bumpers": True,
        "Level 2 - 50 Level Bumpers": False,
        "Level 2 - 75 Level Bumpers": False,
        "Level 2 - 100 Level Bumpers": True,
        "Level 2 - Combo 5": True,
        "Level 2 - Chain x2": False,
        "Bonus Booster 2": False,
        "Treasure Bumper 9": True,
        "Treasure Bumper 10": False,
        "Treasure Bumper 11": False,
        "Treasure Bumper 12": False,
        "Treasure Bumper 13": True,
        "Treasure Bumper 14": True,
        "Treasure Bumper 15": True,
        "Treasure Bumper 16": False,
        "Level 3 - 800 Points": True,
        "Level 3 - 1600 Points": True,
        "Level 3 - 2400 Points": True,
        "Level 3 - 3200 Points": True,
        "Level 3 - 2000 Level Points": False,
        "Level 3 - 4000 Level Points": True,
        "Level 3 - 6000 Level Points": False,
        "Level 3 - 8000 Level Points": True,
        "Level 3 - 25 Level Bumpers": True,
        "Level 3 - 50 Level Bumpers": True,
        "Level 3 - 75 Level Bumpers": True,
        "Level 3 - 100 Level Bumpers": True,
        "Level 3 - 125 Level Bumpers": True,
        "Level 3 - Combo 5": True,
        "Level 3 - Combo 7": True,
        "Level 3 - Chain x2": True,
        "Level 3 - All Clear, 3 colors": False,
        "Bonus Booster 3": False,
        "Treasure Bumper 17": True,
        "Treasure Bumper 18": False,
        "Treasure Bumper 19": False,
        "Treasure Bumper 20": False,
        "Treasure Bumper 21": True,
        "Treasure Bumper 22": True,
        "Treasure Bumper 23": False,
        "Treasure Bumper 24": False,
        "Level 4 - 1500 Points": True,
        "Level 4 - 3000 Points": False,
        "Level 4 - 4500 Points": False,
        "Level 4 - 6000 Points": True,
        "Level 4 - 3000 Level Points": False,
        "Level 4 - 6000 Level Points": False,
        "Level 4 - 9000 Level Points": True,
        "Level 4 - 12000 Level Points": False,
        "Level 4 - 25 Level Bumpers": True,
        "Level 4 - 50 Level Bumpers": False,
        "Level 4 - 75 Level Bumpers": False,
        "Level 4 - 100 Level Bumpers": False,
        "Level 4 - 125 Level Bumpers": True,
        "Level 4 - 150 Level Bumpers": True,
        "Level 4 - Combo 5": True,
        "Level 4 - Combo 7": True,
        "Level 4 - Chain x2": False,
        "Level 4 - Chain x3": False,
        "Bonus Booster 4": False,
        "Bonus Booster 5": True,
        "Treasure Bumper 25": True,
        "Treasure Bumper 26": True,
        "Treasure Bumper 27": True,
        "Treasure Bumper 28": True,
        "Treasure Bumper 29": True,
        "Treasure Bumper 30": False,
        "Treasure Bumper 31": True,
        "Treasure Bumper 32": True,
        "Level 5 - 50,000+ Total Points": True,
        "Level 5 - Cleared all Hazards": False,
    }

    def __init__(self, multiworld: "MultiWorld", player: int):
        super().__init__(multiworld, player)
        # Game-specific world attributes
        self.world_class_name = 'BumpStikWorld'
        self.task_advances = 4
        self.turners = 3
        self.paint_cans = 3
        self.traps = 5
        self.rainbow_trap_weight = 50
        self.spinner_trap_weight = 50
        self.killer_trap_weight = 0
        self.world_description = "Bumper Stickers is a match-three puzzle game unlike any you've seen.\nLaunch Bumpers onto the field, and match them in sets of three of the same color.\nHow long can you go without getting Jammed?"

    # Canonical seed for deterministic placement
    CANONICAL_SEED: ClassVar[int] = 1

    def generate_early(self) -> None:
        """Push starting items and load canonical options for canonical seed."""
        self._push_starting_items()
        if self.multiworld.seed == self.CANONICAL_SEED:
            self.options.randomize_items.value = False
            if self.options.use_canonical_options.value:
                self._load_canonical_options()

    def _load_canonical_options(self) -> None:
        """Load options from _worldgen_options.json for canonical seed generation.

        This ensures that when generating the canonical seed, the same options are used
        as in the original export, producing identical output.
        """
        # Find the options file in the same directory as this module
        world_dir = os.path.dirname(os.path.abspath(__file__))
        options_path = os.path.join(world_dir, '_worldgen_options.json')

        if not os.path.exists(options_path):
            return  # No options file, use defaults

        try:
            with open(options_path, 'r') as f:
                options_data = json.load(f)
        except (json.JSONDecodeError, IOError):
            return  # Can't read options, use defaults

        if not options_data:
            return

        # Map option names from JSON (snake_case) to option attributes
        for option_name, option_value in options_data.items():
            # Get the option attribute if it exists
            if not hasattr(self.options, option_name):
                continue

            option_obj = getattr(self.options, option_name)

            # Handle different option types
            if isinstance(option_value, bool):
                # Toggle options - preserve as boolean to match original world behavior
                # (Original worlds set value = False directly, not value = 0)
                option_obj.value = option_value
            elif isinstance(option_value, int):
                # Range or Choice options with numeric value
                option_obj.value = option_value
            elif isinstance(option_value, str):
                # Choice options with string value - need to look up the value
                # Try to find the corresponding option_* attribute
                option_attr_name = f"option_{option_value}"
                if hasattr(option_obj.__class__, option_attr_name):
                    option_obj.value = getattr(option_obj.__class__, option_attr_name)
                else:
                    # Try to use the string directly if the class has a from_text method
                    try:
                        option_obj.value = option_obj.__class__.from_text(option_value).value
                    except (ValueError, KeyError, AttributeError):
                        pass  # Keep existing value

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]

            # Check for mixed classification items (e.g., some progression, some filler)
            classification_counts = getattr(item_data, 'classification_counts', None)
            if classification_counts:
                # Create items with per-classification counts
                classification_map = {
                    'progression': ItemClassification.progression,
                    'progression_skip_balancing': ItemClassification.progression_skip_balancing,
                    'useful': ItemClassification.useful,
                    'trap': ItemClassification.trap,
                    'filler': ItemClassification.filler,
                }
                for classification_name, class_count in classification_counts.items():
                    classification = classification_map.get(classification_name, ItemClassification.filler)
                    for _ in range(class_count):
                        item = BumperStickersWorldGenItem(
                            item_name,
                            classification,
                            item_data.id,
                            self.player
                        )
                        item_pool.append(item)
            else:
                # Standard case: all items have the same classification
                for _ in range(count):
                    item = BumperStickersWorldGenItem(
                        item_name,
                        item_data.classification,
                        item_data.id,
                        self.player
                    )
                    item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = BumperStickersWorldGenItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)
                # If the location is an event, mark the item as an event too
                # (matches original world behavior where item.code = None for events)
                if getattr(location, 'event', False) or location.address is None:
                    item.code = None

    def _push_starting_items(self) -> None:
        """Push starting items as precollected (for state counters like coins)."""
        for item_name, count in STARTING_ITEMS.items():
            if item_name in item_table:
                for _ in range(count):
                    item = self.create_item(item_name)
                    self.multiworld.push_precollected(item)

    def pre_fill(self) -> None:
        """Pre-fill items if not randomizing or when tracking.

        During tracking (generation_is_fake=True), we always place canonical items
        so that location_item_name() checks work correctly for self-locking rules.
        """
        if not self.options.randomize_items.value or getattr(self.multiworld, 'generation_is_fake', False):
            self._place_original_items()

    def _place_original_items(self) -> None:
        """Place items in their canonical locations when not randomized.

        Process advancement locations first to ensure they get advancement items.
        This is critical for cross-validation in spoiler tests, where item
        advancement flags determine whether items are counted.
        """
        # Two-pass placement: first advancement locations, then the rest
        advancement_locs = getattr(self, 'advancement_locations', set())

        # Sort locations to process advancement locations first
        sorted_placements = sorted(
            self.canonical_placements.items(),
            key=lambda x: 0 if x[0] in advancement_locs else 1
        )

        for location_name, item_name in sorted_placements:
            location = self.multiworld.get_location(location_name, self.player)

            # Skip if already filled (e.g., by _place_locked_items or generate_basic)
            if location.item is not None:
                continue

            # Check if we have expected advancement status for this location (for mixed-class items)
            # This ensures we match the original's progression distribution
            expected_advancement = None
            if hasattr(self, 'canonical_placement_advancements'):
                expected_advancement = self.canonical_placement_advancements.get(location_name)

            # Try to find and use an item from the pool (preserves correct classification)
            # Note: Must use index-based removal because Item.__eq__ only compares name/player,
            # not classification, so list.remove() would remove the wrong item
            item = None
            progression_idx = None
            filler_idx = None

            for idx, pool_item in enumerate(self.multiworld.itempool):
                if pool_item.name == item_name and pool_item.player == self.player:
                    if pool_item.advancement:
                        if progression_idx is None:
                            progression_idx = idx
                    else:
                        if filler_idx is None:
                            filler_idx = idx

                    # If we found both types, stop searching
                    if progression_idx is not None and filler_idx is not None:
                        break

            # Select item based on expected advancement status or fall back to progression-first
            if expected_advancement is True and progression_idx is not None:
                chosen_idx = progression_idx
            elif expected_advancement is False and filler_idx is not None:
                chosen_idx = filler_idx
            elif progression_idx is not None:
                # Default: prefer progression
                chosen_idx = progression_idx
            else:
                chosen_idx = filler_idx

            if chosen_idx is not None:
                item = self.multiworld.itempool.pop(chosen_idx)
            else:
                # Fall back to creating a new item if not found in pool
                item = self.create_item(item_name)

            # Place item without setting locked=True, so the exporter writes
            # locked=false in rules.json (matching the original world's behavior).
            # place_locked_item() would mark these as locked, causing the frontend
            # spoiler test to skip them.
            location.item = item
            item.location = location

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        item = BumperStickersWorldGenItem(name, data.classification, data.id, self.player)
        return item


    def fill_slot_data(self) -> Dict[str, Any]:
        """Return data for the client."""
        return {}
