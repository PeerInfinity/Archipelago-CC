"""
Lufia II Ancient Cave Test world implementation for Archipelago.

Auto-generated by world_generator.
"""

from typing import ClassVar, Dict
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

from .Items import item_table, LufiaIIAncientCaveTestItem
from .Locations import location_table, LufiaIIAncientCaveTestLocation
from .Options import LufiaIIAncientCaveTestOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "Agony helm": 3,
    "Air whip": 1,
    "Aqua helm": 1,
    "Boom turban": 2,
    "Catfish jwl.": 1,
    "Cryst shield": 1,
    "Dark mirror": 2,
    "Dia ring": 1,
    "Dragon spear": 3,
    "Earth jewel": 1,
    "Flame shield": 1,
    "Gades blade": 1,
    "Silver eye": 1,
    "Sizzle sword": 2,
    "Sky sword": 1,
    "Snow sword": 2,
    "Thundo jewel": 1,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {
    "Boss": "Ancient key",
    "Chest access 11-15": "Progressive chest access",
    "Chest access 16-20": "Progressive chest access",
    "Chest access 21-25": "Progressive chest access",
    "Chest access 6-10": "Progressive chest access",
    "Final Floor reached": "Final Floor access",
    "Iris treasure 1": "Iris sword",
    "Iris treasure 2": "Iris shield",
    "Iris treasure 3": "Iris helmet",
    "Iris treasure 4": "Iris armor",
    "Iris treasure 5": "Iris ring",
    "Iris treasure 6": "Iris jewel",
    "Iris treasure 7": "Iris staff",
    "Iris treasure 8": "Iris pot",
    "Iris treasure 9": "Iris tiara",
}


class LufiaIIAncientCaveTestWeb(WebWorld):
    """Web interface for Lufia II Ancient Cave Test."""
    theme = "ocean"
    tutorials = []


class LufiaIIAncientCaveTestWorld(RuleWorldMixin, World):
    """
    Lufia II Ancient Cave Test for Archipelago.

    Auto-generated world implementation.
    """

    game: ClassVar[str] = "Lufia II Ancient Cave Test"
    web: ClassVar[WebWorld] = LufiaIIAncientCaveTestWeb()

    options_dataclass = LufiaIIAncientCaveTestOptions
    options: LufiaIIAncientCaveTestOptions

    # Disable rule caching - requires CollectionState.rule_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]
            for _ in range(count):
                item = LufiaIIAncientCaveTestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = LufiaIIAncientCaveTestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        return LufiaIIAncientCaveTestItem(name, data.classification, data.id, self.player)

    def fill_slot_data(self) -> Dict[str, object]:
        """Return data for the client."""
        return {}
