"""
Region definitions for Mega Man 3 WorldGen.

Auto-generated by world_generator.
"""

from typing import Dict, Set
from BaseClasses import MultiWorld, Region, Entrance
from .Locations import location_table, MegaMan3WorldGenLocation


def create_regions(multiworld: MultiWorld, player: int) -> None:
    """Create all regions, locations, and connections."""

    # Create all regions
    region_names = ["Menu", "Needle Man Stage", "Magnet Man Stage", "Gemini Man Stage", "Hard Man Stage", "Top Man Stage", "Snake Man Stage", "Spark Man Stage", "Shadow Man Stage", "Doc Robot (Needle) - Air", "Doc Robot (Needle) - Crash", "Doc Robot (Gemini) - Flash", "Doc Robot (Gemini) - Bubble", "Doc Robot (Shadow) - Wood", "Doc Robot (Shadow) - Heat", "Doc Robot (Spark) - Metal", "Doc Robot (Spark) - Quick", "Break Man", "Wily Stage 1", "Wily Stage 2", "Wily Stage 3", "Wily Stage 4", "Wily Stage 5", "Wily Stage 6"]

    regions = {}
    for region_name in region_names:
        hint = None
        region = Region(region_name, player, multiworld, hint)
        regions[region_name] = region

    # Mark dynamically added regions (these won't appear in sphere log comparisons)
    try:
        _dynamically_added = DYNAMICALLY_ADDED_REGIONS
    except NameError:
        _dynamically_added = set()
    for region_name in _dynamically_added:
        if region_name in regions:
            regions[region_name].dynamically_added = True

    # Add locations to regions
    for location_name, location_data in location_table.items():
        region = regions[location_data.region]
        location = MegaMan3WorldGenLocation(
            player,
            location_name,
            location_data.location_id,
            region
        )

        # Apply location properties from location_data
        if location_data.event:
            location.event = True
        if location_data.progress_type is not None:
            location.progress_type = location_data.progress_type
        if not location_data.show_in_spoiler:
            location.show_in_spoiler = False

        region.locations.append(location)

    # Create entrances
    _create_entrance(regions["Menu"], regions["Needle Man Stage"], "To Needle Man Stage")
    _create_entrance(regions["Menu"], regions["Magnet Man Stage"], "To Magnet Man Stage")
    _create_entrance(regions["Menu"], regions["Gemini Man Stage"], "To Gemini Man Stage")
    _create_entrance(regions["Menu"], regions["Hard Man Stage"], "To Hard Man Stage")
    _create_entrance(regions["Menu"], regions["Top Man Stage"], "To Top Man Stage")
    _create_entrance(regions["Menu"], regions["Snake Man Stage"], "To Snake Man Stage")
    _create_entrance(regions["Menu"], regions["Spark Man Stage"], "To Spark Man Stage")
    _create_entrance(regions["Menu"], regions["Shadow Man Stage"], "To Shadow Man Stage")
    _create_entrance(regions["Menu"], regions["Doc Robot (Needle) - Air"], "To Doc Robot (Needle) - Air")
    _create_entrance(regions["Menu"], regions["Doc Robot (Gemini) - Flash"], "To Doc Robot (Gemini) - Flash")
    _create_entrance(regions["Menu"], regions["Doc Robot (Shadow) - Wood"], "To Doc Robot (Shadow) - Wood")
    _create_entrance(regions["Menu"], regions["Doc Robot (Spark) - Metal"], "To Doc Robot (Spark) - Metal")
    _create_entrance(regions["Menu"], regions["Break Man"], "To Break Man")
    _create_entrance(regions["Doc Robot (Needle) - Air"], regions["Doc Robot (Needle) - Crash"], "To Doc Robot (Needle) - Crash")
    _create_entrance(regions["Doc Robot (Gemini) - Flash"], regions["Doc Robot (Gemini) - Bubble"], "To Doc Robot (Gemini) - Bubble")
    _create_entrance(regions["Doc Robot (Shadow) - Wood"], regions["Doc Robot (Shadow) - Heat"], "To Doc Robot (Shadow) - Heat")
    _create_entrance(regions["Doc Robot (Spark) - Metal"], regions["Doc Robot (Spark) - Quick"], "To Doc Robot (Spark) - Quick")
    _create_entrance(regions["Break Man"], regions["Wily Stage 1"], "To Wily Stage 1")
    _create_entrance(regions["Wily Stage 1"], regions["Wily Stage 2"], "To Wily Stage 2")
    _create_entrance(regions["Wily Stage 2"], regions["Wily Stage 3"], "To Wily Stage 3")
    _create_entrance(regions["Wily Stage 3"], regions["Wily Stage 4"], "To Wily Stage 4")
    _create_entrance(regions["Wily Stage 4"], regions["Wily Stage 5"], "To Wily Stage 5")
    _create_entrance(regions["Wily Stage 5"], regions["Wily Stage 6"], "To Wily Stage 6")

    # Add all regions to multiworld
    # Regions must be added even if they have no locations or exits, because:
    # 1. They may be targets of entrances from other regions
    # 2. They may be referenced by CanReachRegion() rules
    for region in regions.values():
        multiworld.regions.append(region)


def _create_entrance(source: Region, target: Region, name: str) -> Entrance:
    """Helper to create and connect an entrance."""
    entrance = Entrance(source.player, name, source)
    entrance.connect(target)
    source.exits.append(entrance)
    return entrance
