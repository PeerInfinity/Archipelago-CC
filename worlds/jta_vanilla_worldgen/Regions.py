"""
Region definitions for Journey to Ascension Vanilla WorldGen.

Auto-generated by world_generator.
"""

from typing import Dict, Set
from BaseClasses import MultiWorld, Region, Entrance
from .Locations import location_table, JourneytoAscensionVanillaWorldGenLocation


def create_regions(multiworld: MultiWorld, player: int) -> None:
    """Create all regions, locations, and connections."""

    # Create all regions
    region_names = ["Menu", "The Village", "The Village Watch", "The Raid", "The Wilderness", "The Cave System", "The Road to the City", "The City Outskirts", "The City", "The Forest", "The Magician", "The Ocean", "The Island", "The Desert", "The Oasis", "The Ritual", "The Dream"]

    regions = {}
    for region_name in region_names:
        hint = None
        region = Region(region_name, player, multiworld, hint)
        regions[region_name] = region

    # Mark dynamically added regions (these won't appear in sphere log comparisons)
    try:
        _dynamically_added = DYNAMICALLY_ADDED_REGIONS
    except NameError:
        _dynamically_added = set()
    for region_name in _dynamically_added:
        if region_name in regions:
            regions[region_name].dynamically_added = True

    # Add locations to regions
    for location_name, location_data in location_table.items():
        region = regions[location_data.region]
        location = JourneytoAscensionVanillaWorldGenLocation(
            player,
            location_name,
            location_data.location_id,
            region
        )

        # Apply location properties from location_data
        if location_data.event:
            location.event = True
        if location_data.progress_type is not None:
            location.progress_type = location_data.progress_type
        if not location_data.show_in_spoiler:
            location.show_in_spoiler = False

        region.locations.append(location)

    # Create entrances
    _create_entrance(regions["Menu"], regions["The Village"], "Start Journey")
    _create_entrance(regions["The Village"], regions["The Village Watch"], "The Village -> The Village Watch")
    _create_entrance(regions["The Village Watch"], regions["The Raid"], "The Village Watch -> The Raid")
    _create_entrance(regions["The Raid"], regions["The Wilderness"], "The Raid -> The Wilderness")
    _create_entrance(regions["The Wilderness"], regions["The Cave System"], "The Wilderness -> The Cave System")
    _create_entrance(regions["The Cave System"], regions["The Road to the City"], "The Cave System -> The Road to the City")
    _create_entrance(regions["The Road to the City"], regions["The City Outskirts"], "The Road to the City -> The City Outskirts")
    _create_entrance(regions["The City Outskirts"], regions["The City"], "The City Outskirts -> The City")
    _create_entrance(regions["The City"], regions["The Forest"], "The City -> The Forest")
    _create_entrance(regions["The Forest"], regions["The Magician"], "The Forest -> The Magician")
    _create_entrance(regions["The Magician"], regions["The Ocean"], "The Magician -> The Ocean")
    _create_entrance(regions["The Ocean"], regions["The Island"], "The Ocean -> The Island")
    _create_entrance(regions["The Island"], regions["The Desert"], "The Island -> The Desert")
    _create_entrance(regions["The Desert"], regions["The Oasis"], "The Desert -> The Oasis")
    _create_entrance(regions["The Oasis"], regions["The Ritual"], "The Oasis -> The Ritual")
    _create_entrance(regions["The Ritual"], regions["The Dream"], "The Ritual -> The Dream")

    # Add all regions to multiworld
    # Regions must be added even if they have no locations or exits, because:
    # 1. They may be targets of entrances from other regions
    # 2. They may be referenced by CanReachRegion() rules
    for region in regions.values():
        multiworld.regions.append(region)


def _create_entrance(source: Region, target: Region, name: str) -> Entrance:
    """Helper to create and connect an entrance."""
    entrance = Entrance(source.player, name, source)
    entrance.connect(target)
    source.exits.append(entrance)
    return entrance
