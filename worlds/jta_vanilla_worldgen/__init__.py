"""
Journey to Ascension Vanilla WorldGen world implementation for Archipelago.

Auto-generated by world_generator.
"""
import json
import os

from typing import ClassVar, Dict, List, Set, Any, TYPE_CHECKING
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

if TYPE_CHECKING:
    from BaseClasses import CollectionState, MultiWorld

from .Items import item_table, ItemData, JourneytoAscensionVanillaWorldGenItem
from .Locations import location_table, JourneytoAscensionVanillaWorldGenLocation
from .Options import JourneytoAscensionVanillaWorldGenOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "Attunement": 1,
    "Deep Sea Diving": 1,
    "Energetic Memory": 1,
    "Energetic Spell": 1,
    "Experienced Traveler": 1,
    "Found Lost Temple": 1,
    "Goblin Scourge": 1,
    "High Altitude Climbing": 1,
    "How to Read": 1,
    "How to Write": 1,
    "Minor Time Compression": 1,
    "Mysterious Amulet": 1,
    "Purged Bureaucracy": 1,
    "Reflections on the Journey": 1,
    "Sunken Treasure": 1,
    "The Worm": 1,
    "Underground Connection": 1,
    "Understanding of the Reset": 1,
    "Village Hero": 1,
    "Villager Gratitude": 1,
    "Walk Without Rhythm": 1,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {
    "Reach Goal Zone": "Victory",
}

# Starting items - items the player begins with (precollected)
STARTING_ITEMS: Dict[str, int] = {

}


class JourneytoAscensionVanillaWorldGenWeb(WebWorld):
    """Web interface for Journey to Ascension Vanilla WorldGen."""
    theme = "ocean"
    game_info_languages: List[str] = ['en']
    tutorials = [
        Tutorial(
            "Setup Guide",
            "A guide to setting up Journey to Ascension for Archipelago multiworld.",
            "English",
            "setup_en.md",
            "setup/en",
            ["Archipelago Team"]
        )
    ]


class JTAWorld(RuleWorldMixin, World):
    """
    Journey to Ascension is an incremental/idle game where you progress
    through zones by completing tasks, leveling skills, and collecting perks.
    This randomizer shuffles perk placements across tasks.
    """

    game: ClassVar[str] = "Journey to Ascension Vanilla WorldGen"
    web: ClassVar[WebWorld] = JourneytoAscensionVanillaWorldGenWeb()

    options_dataclass = JourneytoAscensionVanillaWorldGenOptions
    options: JourneytoAscensionVanillaWorldGenOptions

    # Disable rule caching - requires CollectionState.rule_builder_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    # Expose item_table as item_name_to_item for exporter compatibility
    # This allows the exporter handler to find item classifications
    item_name_to_item: ClassVar[Dict[str, "ItemData"]] = item_table

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    item_name_groups: ClassVar[Dict[str, frozenset]] = {
        "Everything": frozenset(["How to Read", "How to Write", "Villager Gratitude", "Village Hero", "Mysterious Amulet", "Energetic Spell", "Goblin Scourge", "Experienced Traveler", "Understanding of the Reset", "Underground Connection", "Minor Time Compression", "Purged Bureaucracy", "High Altitude Climbing", "Attunement", "Sunken Treasure", "Deep Sea Diving", "Found Lost Temple", "Walk Without Rhythm", "The Worm", "Reflections on the Journey", "Energetic Memory", "Awakening", "Tower of Babel", "Dream Prism", "Major Time Compression", "Headmaster", "Hide in Plain Sight", "Dragon Killing Plan", "Dragon Slayer", "Unified Theory of Magic", "Gazed Beyond the Veil", "Overcame Fear of Skydiving", "Destroyed the Ring", "Purged Demonic Influences", "Studied Underground Forge", "Understanding Leviathan", "Communed with Damned Souls", "Defied the Gods", "Survived the Void", "Divine Power", "Energy Boost #1", "Starting Perk Bonus #1", "Starting Perk Bonus #2", "Starting Perk Bonus #3", "Starting Perk Bonus #4", "Starting Perk Bonus #5", "Starting Perk Bonus #6", "Starting Perk Bonus #7", "Starting Perk Bonus #8", "Starting Perk Bonus #9", "Starting Perk Bonus #10", "Starting Perk Bonus #11", "Starting Perk Bonus #12", "Starting Perk Bonus #13", "Starting Perk Bonus #14", "Starting Perk Bonus #15"]),
    }

    # Placements match the original non-randomized game
    is_vanilla: ClassVar[bool] = True
    # Placements are deterministically reproduced by world generator
    is_canonical: ClassVar[bool] = True

    # Canonical item placements - where items belong in the "vanilla" game
    # Used by exporter to distinguish canonical placements from always-locked items
    canonical_placements: ClassVar[Dict[str, str]] = {
        "Learn How to Read": "How to Read",
        "Learn How to Write": "How to Write",
        "Rescue Villager": "Villager Gratitude",
        "Save the Village": "Village Hero",
        "Find an Amulet": "Mysterious Amulet",
        "Try Casting a Spell": "Energetic Spell",
        "Wipe Out Goblins": "Goblin Scourge",
        "Get Used to Traveling": "Experienced Traveler",
        "Study the Amulet": "Understanding of the Reset",
        "Negotiate with a Rogue Guard": "Underground Connection",
        "Cast a Spell": "Minor Time Compression",
        "Purge Corrupt Bureaucracy": "Purged Bureaucracy",
        "Scale the Mountain": "High Altitude Climbing",
        "Figure Out How to Attune": "Attunement",
        "Dive as a Squid": "Sunken Treasure",
        "Explore Kraken's Lair": "Deep Sea Diving",
        "Explore the Jungle": "Found Lost Temple",
        "Avoid Notice by the Sandworm": "Walk Without Rhythm",
        "Learn to Dance the Worm": "The Worm",
        "Reflect on the Journey": "Reflections on the Journey",
        "Practice Memorization": "Energetic Memory",
        "Reach Goal Zone": "Victory",
    }

    # Canonical placement advancement status - for items with mixed classifications
    # True = progression, False = useful/filler. Used to select correct item copy during placement.
    canonical_placement_advancements: ClassVar[Dict[str, bool]] = {
        "Learn How to Read": True,
        "Learn How to Write": True,
        "Rescue Villager": True,
        "Save the Village": True,
        "Find an Amulet": True,
        "Try Casting a Spell": True,
        "Wipe Out Goblins": True,
        "Get Used to Traveling": True,
        "Study the Amulet": True,
        "Negotiate with a Rogue Guard": True,
        "Cast a Spell": True,
        "Purge Corrupt Bureaucracy": True,
        "Scale the Mountain": True,
        "Figure Out How to Attune": True,
        "Dive as a Squid": True,
        "Explore Kraken's Lair": True,
        "Explore the Jungle": True,
        "Avoid Notice by the Sandworm": True,
        "Learn to Dance the Worm": True,
        "Reflect on the Journey": True,
        "Practice Memorization": True,
        "Reach Goal Zone": True,
    }

    def __init__(self, multiworld: "MultiWorld", player: int):
        super().__init__(multiworld, player)
        # Game-specific world attributes
        self.world_class_name = 'JTAWorld'
        self.is_vanilla = True
        self.world_description = 'Journey to Ascension is an incremental/idle game where you progress\nthrough zones by completing tasks, leveling skills, and collecting perks.\nThis randomizer shuffles perk placements across tasks.'
        self.slot_data = {'game': 'Journey to Ascension', 'version': '0.5.0', 'goalZone': 15, 'resetsPerSphere': 5, 'costGenFactors': {'itemCollection': True, 'pushCollect': True, 'grindWithPushCollect': True, 'artifacts': True}, 'automation': {'autoQueue': True, 'autoReset': True, 'drainStrategy': True, 'loadoutSequencing': True}, 'perkPlacements': {13: 'How to Read', 27: 'How to Write', 34: 'Villager Gratitude', 37: 'Village Hero', 43: 'Mysterious Amulet', 54: 'Energetic Spell', 58: 'Goblin Scourge', 64: 'Experienced Traveler', 69: 'Understanding of the Reset', 74: 'Underground Connection', 84: 'Minor Time Compression', 89: 'Purged Bureaucracy', 90: 'High Altitude Climbing', 104: 'Attunement', 114: 'Sunken Treasure', 118: 'Deep Sea Diving', 124: 'Found Lost Temple', 134: 'Walk Without Rhythm', 138: 'The Worm', 144: 'Reflections on the Journey', 155: 'Energetic Memory'}}

    # Canonical seed for deterministic placement
    CANONICAL_SEED: ClassVar[int] = 1

    def generate_early(self) -> None:
        """Push starting items and load canonical options for canonical seed."""
        self._push_starting_items()
        # Set preset_label for exporter: use class-level label as base, append seed/vanilla suffix
        base_label = getattr(self.__class__, 'preset_label', '')
        if base_label:
            base = base_label.split()[0] if ' ' in base_label else base_label
            is_vanilla = getattr(self.__class__, 'is_vanilla', False)
            self.preset_label = f"{base} v" if is_vanilla else f"{base} s{self.multiworld.seed}"
        if self.multiworld.seed == self.CANONICAL_SEED:
            self.options.randomize_items.value = False
            if self.options.use_canonical_options.value:
                self._load_canonical_options()

    def _load_canonical_options(self) -> None:
        """Load options from _worldgen_options.json for canonical seed generation.

        This ensures that when generating the canonical seed, the same options are used
        as in the original export, producing identical output.
        """
        # Find the options file in the same directory as this module
        world_dir = os.path.dirname(os.path.abspath(__file__))
        options_path = os.path.join(world_dir, '_worldgen_options.json')

        if not os.path.exists(options_path):
            return  # No options file, use defaults

        try:
            with open(options_path, 'r') as f:
                options_data = json.load(f)
        except (json.JSONDecodeError, IOError):
            return  # Can't read options, use defaults

        if not options_data:
            return

        # Map option names from JSON (snake_case) to option attributes
        for option_name, option_value in options_data.items():
            # Get the option attribute if it exists
            if not hasattr(self.options, option_name):
                continue

            option_obj = getattr(self.options, option_name)

            # Handle different option types
            if isinstance(option_value, bool):
                # Toggle options - preserve as boolean to match original world behavior
                # (Original worlds set value = False directly, not value = 0)
                option_obj.value = option_value
            elif isinstance(option_value, int):
                # Range or Choice options with numeric value
                option_obj.value = option_value
            elif isinstance(option_value, str):
                # Choice options with string value - need to look up the value
                # Try to find the corresponding option_* attribute
                option_attr_name = f"option_{option_value}"
                if hasattr(option_obj.__class__, option_attr_name):
                    option_obj.value = getattr(option_obj.__class__, option_attr_name)
                else:
                    # Try to use the string directly if the class has a from_text method
                    try:
                        option_obj.value = option_obj.__class__.from_text(option_value).value
                    except (ValueError, KeyError, AttributeError):
                        pass  # Keep existing value

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]

            # Check for mixed classification items (e.g., some progression, some filler)
            classification_counts = getattr(item_data, 'classification_counts', None)
            if classification_counts:
                # Create items with per-classification counts
                classification_map = {
                    'progression': ItemClassification.progression,
                    'progression_skip_balancing': ItemClassification.progression_skip_balancing,
                    'useful': ItemClassification.useful,
                    'trap': ItemClassification.trap,
                    'filler': ItemClassification.filler,
                }
                for classification_name, class_count in classification_counts.items():
                    classification = classification_map.get(classification_name, ItemClassification.filler)
                    for _ in range(class_count):
                        item = JourneytoAscensionVanillaWorldGenItem(
                            item_name,
                            classification,
                            item_data.id,
                            self.player
                        )
                        item_pool.append(item)
            else:
                # Standard case: all items have the same classification
                for _ in range(count):
                    item = JourneytoAscensionVanillaWorldGenItem(
                        item_name,
                        item_data.classification,
                        item_data.id,
                        self.player
                    )
                    item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = JourneytoAscensionVanillaWorldGenItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)
                # If the location is an event, mark the item as an event too
                # (matches original world behavior where item.code = None for events)
                if getattr(location, 'event', False) or location.address is None:
                    item.code = None

    def _push_starting_items(self) -> None:
        """Push starting items as precollected (for state counters like coins)."""
        for item_name, count in STARTING_ITEMS.items():
            if item_name in item_table:
                for _ in range(count):
                    item = self.create_item(item_name)
                    self.multiworld.push_precollected(item)

    def generate_basic(self) -> None:
        """Place victory event item and set completion condition."""
        victory_location = self.multiworld.get_location("Reach Goal Zone", self.player)

        # Only place if not already filled (e.g., by _place_original_items)
        if victory_location.item is None:
            victory_item = JourneytoAscensionVanillaWorldGenItem(
                "Victory",
                item_table["Victory"].classification,
                None,
                self.player
            )
            victory_location.place_locked_item(victory_item)

        # Set completion condition
        self.multiworld.completion_condition[self.player] = \
            lambda state: state.has("Victory", self.player)

    def pre_fill(self) -> None:
        """Pre-fill items if not randomizing or when tracking.

        During tracking (generation_is_fake=True), we always place canonical items
        so that location_item_name() checks work correctly for self-locking rules.
        """
        if not self.options.randomize_items.value or getattr(self.multiworld, 'generation_is_fake', False):
            self._place_original_items()

    def _place_original_items(self) -> None:
        """Place items in their canonical locations when not randomized.

        Process advancement locations first to ensure they get advancement items.
        This is critical for cross-validation in spoiler tests, where item
        advancement flags determine whether items are counted.
        """
        # Two-pass placement: first advancement locations, then the rest
        advancement_locs = getattr(self, 'advancement_locations', set())

        # Sort locations to process advancement locations first
        sorted_placements = sorted(
            self.canonical_placements.items(),
            key=lambda x: 0 if x[0] in advancement_locs else 1
        )

        for location_name, item_name in sorted_placements:
            location = self.multiworld.get_location(location_name, self.player)

            # Skip if already filled (e.g., by _place_locked_items or generate_basic)
            if location.item is not None:
                continue

            # Check if we have expected advancement status for this location (for mixed-class items)
            # This ensures we match the original's progression distribution
            expected_advancement = None
            if hasattr(self, 'canonical_placement_advancements'):
                expected_advancement = self.canonical_placement_advancements.get(location_name)

            # Try to find and use an item from the pool (preserves correct classification)
            # Note: Must use index-based removal because Item.__eq__ only compares name/player,
            # not classification, so list.remove() would remove the wrong item
            item = None
            progression_idx = None
            filler_idx = None

            for idx, pool_item in enumerate(self.multiworld.itempool):
                if pool_item.name == item_name and pool_item.player == self.player:
                    if pool_item.advancement:
                        if progression_idx is None:
                            progression_idx = idx
                    else:
                        if filler_idx is None:
                            filler_idx = idx

                    # If we found both types, stop searching
                    if progression_idx is not None and filler_idx is not None:
                        break

            # Select item based on expected advancement status or fall back to progression-first
            if expected_advancement is True and progression_idx is not None:
                chosen_idx = progression_idx
            elif expected_advancement is False and filler_idx is not None:
                chosen_idx = filler_idx
            elif progression_idx is not None:
                # Default: prefer progression
                chosen_idx = progression_idx
            else:
                chosen_idx = filler_idx

            if chosen_idx is not None:
                item = self.multiworld.itempool.pop(chosen_idx)
            else:
                # Fall back to creating a new item if not found in pool
                item = self.create_item(item_name)

            # Place item without setting locked=True, so the exporter writes
            # locked=false in rules.json (matching the original world's behavior).
            # place_locked_item() would mark these as locked, causing the frontend
            # spoiler test to skip them.
            location.item = item
            item.location = location

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        item = JourneytoAscensionVanillaWorldGenItem(name, data.classification, data.id, self.player)
        return item


    def fill_slot_data(self) -> Dict[str, Any]:
        """Return data for the client."""
        return {
            "game": "Journey to Ascension",
            "version": "0.5.0",
            "goalZone": 15,
            "resetsPerSphere": 5,
            "costGenFactors": {'itemCollection': True, 'pushCollect': True, 'grindWithPushCollect': True, 'artifacts': True},
            "automation": {'autoQueue': True, 'autoReset': True, 'drainStrategy': True, 'loadoutSequencing': True},
            "perkPlacements": {13: 'How to Read', 27: 'How to Write', 34: 'Villager Gratitude', 37: 'Village Hero', 43: 'Mysterious Amulet', 54: 'Energetic Spell', 58: 'Goblin Scourge', 64: 'Experienced Traveler', 69: 'Understanding of the Reset', 74: 'Underground Connection', 84: 'Minor Time Compression', 89: 'Purged Bureaucracy', 90: 'High Altitude Climbing', 104: 'Attunement', 114: 'Sunken Treasure', 118: 'Deep Sea Diving', 124: 'Found Lost Temple', 134: 'Walk Without Rhythm', 138: 'The Worm', 144: 'Reflections on the Journey', 155: 'Energetic Memory'},
        }
