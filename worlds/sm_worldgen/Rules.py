"""
Access rules for Super Metroid WorldGen.

Auto-generated by world_generator.
Uses simple Rule Builder rules that the frontend can evaluate directly.
"""

from typing import TYPE_CHECKING

from rule_builder import True_, CanReachRegion

if TYPE_CHECKING:
    from BaseClasses import CollectionState
    from worlds.AutoWorld import World


def set_rules(world: "World") -> None:
    """Set access rules for all locations and entrances.

    For SM WorldGen, we use simplified rules that allow the Fill algorithm to place
    items based on basic region reachability. The actual VARIA logic is handled by
    the frontend during spoiler verification.
    """
    player = world.player
    multiworld = world.multiworld

    # Set completion condition
    multiworld.completion_condition[player] = lambda state: state.has('Mother Brain', player)

    # Note: Entrance rules default to True_() which allows the Fill to work.
    # Location rules use CanReachRegion() which provides basic accessibility.
    # The exporter will extract and export the actual Rule Builder rules.
    pass  # No explicit rules needed - defaults handle basic accessibility
