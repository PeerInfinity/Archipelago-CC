"""
Inscryption Test world implementation for Archipelago.

Auto-generated by world_generator.
"""

from typing import ClassVar, Dict
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

from .Items import item_table, InscryptionTestItem
from .Locations import location_table, InscryptionTestLocation
from .Options import InscryptionTestOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation
ITEMPOOL_COUNTS: Dict[str, int] = {
    "Ancient Obol": 1,
    "Angler Hook": 1,
    "Ant Cards": 1,
    "Bee Figurine": 1,
    "Bone Lord Femur": 1,
    "Bone Lord Holo Key": 1,
    "Bone Lord Horn": 1,
    "Caged Wolf Card": 1,
    "Camera Replica": 1,
    "Card Pack": 24,
    "Currency": 25,
    "Dagger": 1,
    "Dock's Clover Plant": 1,
    "Drowned Soul Card": 1,
    "Epitaph Piece": 9,
    "Extra Battery": 1,
    "Extra Candle": 1,
    "Film Roll": 1,
    "Fishbot Card": 1,
    "Gems Module": 1,
    "Great Kraken Card": 1,
    "Greater Smoke": 1,
    "Holo Pelt": 5,
    "Inspectometer Battery": 1,
    "Lonely Wizbot Card": 1,
    "Magnificus Eye": 1,
    "Monocle": 1,
    "Mrs. Bomb's Remote": 1,
    "Mycologists Holo Key": 1,
    "Nano Armor Generator": 1,
    "Oil Painting's Clover Plant": 1,
    "Ourobot Card": 1,
    "Pile Of Meat": 1,
    "Quill": 1,
    "Ring": 1,
    "Salmon Card": 1,
    "Skink Card": 1,
    "Squirrel Totem Head": 1,
    "Stinkbug Card": 1,
    "Stunted Wolf Card": 1,
    "Wardrobe Key": 1,
}


class InscryptionTestWeb(WebWorld):
    """Web interface for Inscryption Test."""
    theme = "ocean"
    tutorials = []


class InscryptionTestWorld(RuleWorldMixin, World):
    """
    Inscryption Test for Archipelago.

    Auto-generated world implementation.
    """

    game: ClassVar[str] = "Inscryption Test"
    web: ClassVar[WebWorld] = InscryptionTestWeb()

    options_dataclass = InscryptionTestOptions
    options: InscryptionTestOptions

    # Disable rule caching - requires CollectionState.rule_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]
            for _ in range(count):
                item = InscryptionTestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                item_pool.append(item)

        self.multiworld.itempool += item_pool

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        return InscryptionTestItem(name, data.classification, data.id, self.player)

    def fill_slot_data(self) -> Dict[str, object]:
        """Return data for the client."""
        return {}
