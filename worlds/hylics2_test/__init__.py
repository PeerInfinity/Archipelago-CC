"""
Hylics 2 Test world implementation for Archipelago.

Auto-generated by world_generator.
"""

from typing import ClassVar, Dict
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

from .Items import item_table, Hylics2TestItem
from .Locations import location_table, Hylics2TestLocation
from .Options import Hylics2TestOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "100 Bones": 1,
    "50 Bones": 1,
    "BANANA": 14,
    "BOMBO - GENESIS": 1,
    "BOOTS": 1,
    "BOTTOMLESS JUICE": 1,
    "BRAIN DIGITS": 1,
    "BRIDGE KEY": 1,
    "BURRITO": 11,
    "CAVE KEY": 1,
    "CHARGE UP": 1,
    "CLICKER": 1,
    "CLOUD GERM": 3,
    "COFFEE": 1,
    "COFFEE CHIP": 1,
    "CONVERTER WORM": 1,
    "COOKIE": 3,
    "CUPCAKE": 3,
    "CURSED GLOVES": 1,
    "DEEP KEY": 1,
    "DOCK KEY": 1,
    "DUBIOUS BERRY": 1,
    "DUCTILE HABIT": 1,
    "FADED PONCHO": 1,
    "FATE SANDBOX": 1,
    "HOUSE KEY": 1,
    "JAIL KEY": 1,
    "JUICE": 6,
    "JUMPSUIT": 1,
    "LINK MOLLUSC": 1,
    "LONG GLOVES": 5,
    "LOOPED DOME": 2,
    "MATERIEL MITTS": 1,
    "MEAT": 2,
    "MULTI SOUL SPONGE": 6,
    "MULTI STEM CELL": 3,
    "MULTI-COFFEE": 1,
    "MULTI-JUICE": 4,
    "MUSCLE APPLIQUE": 7,
    "NEMATODE INTERFACE": 1,
    "ORGAN FORT": 1,
    "PADDLE": 1,
    "PAPER CUP": 3,
    "PEPTIDE BODKINS": 1,
    "PLEATHER GAGE": 1,
    "PNEUMATOPHORE": 1,
    "POOLWINE": 1,
    "POROMER BLEB": 1,
    "PSYCHIC KNUCKLE": 1,
    "RANCHER PONCHO": 1,
    "SAGE TOKEN": 3,
    "SINGLE GLOVE": 1,
    "SKULL BOMB": 1,
    "SOUL CRISPER": 1,
    "SOUL SPONGE": 6,
    "STEM CELL": 2,
    "TARP": 2,
    "TELEDENUDATE": 1,
    "TELESCOPIC SLEEVE": 1,
    "TENDRIL HAND": 1,
    "TIME SIGIL": 1,
    "TOWER KEY": 1,
    "UPPER CHAMBER KEY": 1,
    "UPPER HOUSE KEY": 1,
    "VESSEL ROOM KEY": 1,
    "WORM ROOM KEY": 1,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {
    "Defeat Gibby": "Victory",
}


class Hylics2TestWeb(WebWorld):
    """Web interface for Hylics 2 Test."""
    theme = "ocean"
    tutorials = []


class Hylics2TestWorld(RuleWorldMixin, World):
    """
    Hylics 2 Test for Archipelago.

    Auto-generated world implementation.
    """

    game: ClassVar[str] = "Hylics 2 Test"
    web: ClassVar[WebWorld] = Hylics2TestWeb()

    options_dataclass = Hylics2TestOptions
    options: Hylics2TestOptions

    # Disable rule caching - requires CollectionState.rule_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    item_name_groups: ClassVar[Dict[str, frozenset]] = {
        "Event": frozenset(["Victory"]),
        "Everything": frozenset(["10 Bones", "100 Bones", "50 Bones", "BANANA", "BOMBO - GENESIS", "BOOTS", "BOTTOMLESS JUICE", "BRAIN DIGITS", "BRIDGE KEY", "BURRITO", "CAVE KEY", "CHARGE UP", "CLICKER", "CLOUD GERM", "COFFEE", "COFFEE CHIP", "CONVERTER WORM", "COOKIE", "CUPCAKE", "CURSED GLOVES", "DEEP KEY", "DOCK KEY", "DUBIOUS BERRY", "DUCTILE HABIT", "Dedusmuln", "FADED PONCHO", "FATE SANDBOX", "HOUSE KEY", "JAIL KEY", "JUICE", "JUMPSUIT", "LINK MOLLUSC", "LONG GLOVES", "LOOPED DOME", "MATERIEL MITTS", "MEAT", "MULTI SOUL SPONGE", "MULTI STEM CELL", "MULTI-COFFEE", "MULTI-JUICE", "MUSCLE APPLIQUE", "NEMATODE INTERFACE", "ORGAN FORT", "PADDLE", "PAPER CUP", "PEPTIDE BODKINS", "PLEATHER GAGE", "PNEUMATOPHORE", "POOLWINE", "POROMER BLEB", "PSYCHIC KNUCKLE", "Pongorma", "RANCHER PONCHO", "SAGE TOKEN", "SINGLE GLOVE", "SKULL BOMB", "SOUL CRISPER", "SOUL SPONGE", "STEM CELL", "Somsnosa", "TARP", "TELEDENUDATE", "TELESCOPIC SLEEVE", "TENDRIL HAND", "TIME SIGIL", "TOWER KEY", "UPPER CHAMBER KEY", "UPPER HOUSE KEY", "VESSEL ROOM KEY", "WORM ROOM KEY"]),
    }

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]
            for _ in range(count):
                item = Hylics2TestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = Hylics2TestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)

    def generate_basic(self) -> None:
        """Place victory event item."""
        victory_location = self.multiworld.get_location("Defeat Gibby", self.player)

        # Only place if not already filled (e.g., by _place_original_items)
        if victory_location.item is None:
            victory_item = Hylics2TestItem(
                "Victory",
                item_table["Victory"].classification,
                None,
                self.player
            )
            victory_location.place_locked_item(victory_item)

        # Set completion condition
        self.multiworld.completion_condition[self.player] = \
            lambda state: state.has("Victory", self.player)

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        return Hylics2TestItem(name, data.classification, data.id, self.player)

    def fill_slot_data(self) -> Dict[str, object]:
        """Return data for the client."""
        return {}
