"""
A Hat in Time WorldGen world implementation for Archipelago.

Auto-generated by world_generator.
"""

from typing import ClassVar, Dict, Any, TYPE_CHECKING
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

if TYPE_CHECKING:
    from BaseClasses import CollectionState, MultiWorld

from .Items import item_table, AHatinTimeWorldGenItem
from .Locations import location_table, AHatinTimeWorldGenLocation
from .Options import AHatinTimeWorldGenOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "100 Pons": 5,
    "25 Pons": 39,
    "50 Pons": 12,
    "Badge Pin": 2,
    "Fast Hatter Badge": 1,
    "Health Pon": 31,
    "Hookshot Badge": 1,
    "Hover Badge": 1,
    "Item Magnet Badge": 1,
    "No Bonk Badge": 1,
    "Projectile Badge": 1,
    "Random Cosmetic": 24,
    "Relic (Blue Crayon)": 1,
    "Relic (Burger Cushion)": 1,
    "Relic (Burger Patty)": 1,
    "Relic (Cool Cow)": 1,
    "Relic (Cow)": 1,
    "Relic (Crayon Box)": 1,
    "Relic (Green Crayon)": 1,
    "Relic (Mountain Set)": 1,
    "Relic (Red Crayon)": 1,
    "Relic (Tin-foil Hat Cow)": 1,
    "Relic (Train)": 1,
    "Relic (UFO)": 1,
    "Scooter Badge": 1,
    "Snatcher's Contract - Mail Delivery Service": 1,
    "Snatcher's Contract - Queen Vanessa's Manor": 1,
    "Snatcher's Contract - The Subcon Well": 1,
    "Snatcher's Contract - Toilet of Doom": 1,
    "Time Piece": 40,
    "Umbrella": 1,
    "Yarn": 50,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {
    "HUMT Access": "HUMT Access",
    "TOD Access": "TOD Access",
    "AFR Access": "AFR Access",
    "Birdhouse Cleared": "Birdhouse Cleared",
    "Lava Cake Cleared": "Lava Cake Cleared",
    "Windmill Cleared": "Windmill Cleared",
    "Twilight Bell Cleared": "Twilight Bell Cleared",
    "TIHS Access": "TIHS Access",
    "Time Piece Cluster": "Time Piece Cluster",
}

# Starting items - items the player begins with (precollected)
STARTING_ITEMS: Dict[str, int] = {
    "Compass Badge": 1,
    "PONS": 2075,
}


class AHatinTimeWorldGenWeb(WebWorld):
    """Web interface for A Hat in Time WorldGen."""
    theme = "ocean"
    tutorials = []


class AHatinTimeWorldGenWorld(RuleWorldMixin, World):
    """
    A Hat in Time WorldGen for Archipelago.

    Auto-generated world implementation.
    """

    game: ClassVar[str] = "A Hat in Time WorldGen"
    web: ClassVar[WebWorld] = AHatinTimeWorldGenWeb()

    options_dataclass = AHatinTimeWorldGenOptions
    options: AHatinTimeWorldGenOptions

    # Disable rule caching - requires CollectionState.rule_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    item_name_groups: ClassVar[Dict[str, frozenset]] = {
        "Everything": frozenset(["Yarn", "Time Piece", "Sprint Hat", "Brewing Hat", "Ice Hat", "Dweller Mask", "Time Stop Hat", "Projectile Badge", "Fast Hatter Badge", "Hover Badge", "Hookshot Badge", "Item Magnet Badge", "No Bonk Badge", "Compass Badge", "Scooter Badge", "One-Hit Hero Badge", "Camera Badge", "Relic (Burger Patty)", "Relic (Burger Cushion)", "Relic (Mountain Set)", "Relic (Train)", "Relic (UFO)", "Relic (Cow)", "Relic (Cool Cow)", "Relic (Tin-foil Hat Cow)", "Relic (Crayon Box)", "Relic (Red Crayon)", "Relic (Blue Crayon)", "Relic (Green Crayon)", "Relic (Cake Stand)", "Relic (Shortcake)", "Relic (Chocolate Cake Slice)", "Relic (Chocolate Cake)", "Relic (Necklace Bust)", "Relic (Necklace)", "25 Pons", "50 Pons", "100 Pons", "Health Pon", "Random Cosmetic", "Baby Trap", "Laser Trap", "Parade Trap", "Badge Pin", "Umbrella", "Progressive Painting Unlock", "Metro Ticket - Yellow", "Metro Ticket - Green", "Metro Ticket - Blue", "Metro Ticket - Pink", "Snatcher's Contract - The Subcon Well", "Snatcher's Contract - Toilet of Doom", "Snatcher's Contract - Queen Vanessa's Manor", "Snatcher's Contract - Mail Delivery Service", "Zipline Unlock - The Birdhouse Path", "Zipline Unlock - The Lava Cake Path", "Zipline Unlock - The Windmill Path", "Zipline Unlock - The Twilight Bell Path"]),
        "Burger": frozenset(["Relic (Burger Patty)", "Relic (Burger Cushion)"]),
        "Train": frozenset(["Relic (Mountain Set)", "Relic (Train)"]),
        "UFO": frozenset(["Relic (UFO)", "Relic (Cow)", "Relic (Cool Cow)", "Relic (Tin-foil Hat Cow)"]),
        "Crayon": frozenset(["Relic (Crayon Box)", "Relic (Red Crayon)", "Relic (Blue Crayon)", "Relic (Green Crayon)"]),
        "Cake": frozenset(["Relic (Cake Stand)", "Relic (Shortcake)", "Relic (Chocolate Cake Slice)", "Relic (Chocolate Cake)"]),
        "Necklace": frozenset(["Relic (Necklace Bust)", "Relic (Necklace)"]),
    }

    # Accumulator rules for state counters (e.g., coins)
    # These tell the exporter how to parse items like "60 coins" -> add 60 to " coins" counter
    accumulator_rules: ClassVar[list] = [
        {"pattern": r"^(\d+) Pons$", "extract_value": True, "target": "PONS", "discriminator": None},
    ]

    # Initial values for prog_items accumulators
    prog_items_init: ClassVar[dict] = {
        "PONS": 0,
    }

    def __init__(self, multiworld: "MultiWorld", player: int):
        super().__init__(multiworld, player)
        # Game-specific world attributes
        self.hat_yarn_costs = {0: 6, 1: 4, 2: 8, 3: 6, 4: 6}
        self.hat_craft_order = [3, 2, 4, 1, 0]

    def generate_early(self) -> None:
        """Push starting items as precollected."""
        self._push_starting_items()

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def collect(self, state: "CollectionState", item: "Item") -> bool:
        """Collect item and track cumulative counters from accumulator rules."""
        import re
        change = super().collect(state, item)
        if change:
            for rule in self.accumulator_rules:
                match = re.match(rule["pattern"], item.name)
                if match:
                    if rule["extract_value"]:
                        value = int(match.group(1))
                    else:
                        value = 1
                    state.prog_items[item.player][rule["target"]] += value
                    break
        return change

    def remove(self, state: "CollectionState", item: "Item") -> bool:
        """Remove item and update cumulative counters from accumulator rules."""
        import re
        change = super().remove(state, item)
        if change:
            for rule in self.accumulator_rules:
                match = re.match(rule["pattern"], item.name)
                if match:
                    if rule["extract_value"]:
                        value = int(match.group(1))
                    else:
                        value = 1
                    state.prog_items[item.player][rule["target"]] -= value
                    break
        return change

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]
            for _ in range(count):
                item = AHatinTimeWorldGenItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = AHatinTimeWorldGenItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)

    def _push_starting_items(self) -> None:
        """Push starting items as precollected (for state counters like coins)."""
        for item_name, count in STARTING_ITEMS.items():
            if item_name in item_table:
                for _ in range(count):
                    item = self.create_item(item_name)
                    self.multiworld.push_precollected(item)

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        return AHatinTimeWorldGenItem(name, data.classification, data.id, self.player)


    def fill_slot_data(self) -> Dict[str, Any]:
        """Return data for the client."""
        return {}
