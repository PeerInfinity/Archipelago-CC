"""
Noita Test world implementation for Archipelago.

Auto-generated by world_generator.
"""

from typing import ClassVar, Dict
from BaseClasses import Item, ItemClassification, Tutorial
from worlds.AutoWorld import WebWorld, World
from rule_builder import RuleWorldMixin

from .Items import item_table, NoitaTestItem
from .Locations import location_table, NoitaTestLocation
from .Options import NoitaTestOptions
from .Regions import create_regions
from .Rules import set_rules


# Item pool counts from original generation (excluding locked placements)
ITEMPOOL_COUNTS: Dict[str, int] = {
    "All-Seeing Eye Perk": 1,
    "Broken Wand": 2,
    "Chaos Die": 4,
    "Electricity Immunity Perk": 1,
    "Explosion Immunity Perk": 1,
    "Extra Life Perk": 2,
    "Extra Max HP": 20,
    "Fire Immunity Perk": 1,
    "Gold (1000)": 1,
    "Gold (200)": 7,
    "Greed Die": 2,
    "Kammi": 2,
    "Melee Immunity Perk": 1,
    "Potion": 13,
    "Powder Pouch": 6,
    "Random Potion": 2,
    "Refreshing Gourd": 1,
    "Secret Potion": 4,
    "Spell Refresher": 6,
    "Sädekivi": 1,
    "Tinker with Wands Everywhere Perk": 1,
    "Toxic Immunity Perk": 1,
    "Trap": 10,
    "Wand (Tier 1)": 2,
    "Wand (Tier 2)": 3,
    "Wand (Tier 3)": 6,
    "Wand (Tier 4)": 4,
    "Wand (Tier 5)": 2,
    "Wand (Tier 6)": 2,
}

# Locked placements - items that must be placed via place_locked_item
LOCKED_PLACEMENTS: Dict[str, str] = {
    "Portal to Holy Mountain 1": "Portal to Holy Mountain 1",
    "Portal to Holy Mountain 2": "Portal to Holy Mountain 2",
    "Portal to Holy Mountain 3": "Portal to Holy Mountain 3",
    "Portal to Holy Mountain 4": "Portal to Holy Mountain 4",
    "Portal to Holy Mountain 5": "Portal to Holy Mountain 5",
    "Portal to Holy Mountain 6": "Portal to Holy Mountain 6",
    "Portal to Holy Mountain 7": "Portal to Holy Mountain 7",
    "Victory": "Victory",
}


class NoitaTestWeb(WebWorld):
    """Web interface for Noita Test."""
    theme = "ocean"
    tutorials = []


class NoitaTestWorld(RuleWorldMixin, World):
    """
    Noita Test for Archipelago.

    Auto-generated world implementation.
    """

    game: ClassVar[str] = "Noita Test"
    web: ClassVar[WebWorld] = NoitaTestWeb()

    options_dataclass = NoitaTestOptions
    options: NoitaTestOptions

    # Disable rule caching - requires CollectionState.rule_cache from PR #5048
    rule_caching_enabled: ClassVar[bool] = False

    item_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.id for name, data in item_table.items() if data.id is not None
    }

    location_name_to_id: ClassVar[Dict[str, int]] = {
        name: data.location_id for name, data in location_table.items()
        if data.location_id is not None
    }

    item_name_groups: ClassVar[Dict[str, frozenset]] = {
        "Event": frozenset(["Portal to Holy Mountain 1", "Portal to Holy Mountain 2", "Portal to Holy Mountain 3", "Portal to Holy Mountain 4", "Portal to Holy Mountain 5", "Portal to Holy Mountain 6", "Portal to Holy Mountain 7", "Victory"]),
        "Everything": frozenset(["All-Seeing Eye Perk", "Broken Wand", "Chaos Die", "Electricity Immunity Perk", "Explosion Immunity Perk", "Extra Life Perk", "Extra Max HP", "Fire Immunity Perk", "Gold (1000)", "Gold (200)", "Greed Die", "Kammi", "Kantele", "Melee Immunity Perk", "Orb", "Potion", "Powder Pouch", "Random Potion", "Refreshing Gourd", "Secret Potion", "Spatial Awareness Perk", "Spell Refresher", "Sädekivi", "Tinker with Wands Everywhere Perk", "Toxic Immunity Perk", "Trap", "Wand (Tier 1)", "Wand (Tier 2)", "Wand (Tier 3)", "Wand (Tier 4)", "Wand (Tier 5)", "Wand (Tier 6)"]),
        "Gold": frozenset(["Gold (1000)", "Gold (200)"]),
        "Items": frozenset(["Broken Wand", "Chaos Die", "Greed Die", "Kammi", "Powder Pouch", "Random Potion", "Refreshing Gourd", "Secret Potion", "Sädekivi"]),
        "Orbs": frozenset(["Orb"]),
        "Perks": frozenset(["All-Seeing Eye Perk", "Electricity Immunity Perk", "Explosion Immunity Perk", "Fire Immunity Perk", "Melee Immunity Perk", "Spatial Awareness Perk", "Tinker with Wands Everywhere Perk", "Toxic Immunity Perk"]),
        "Pickups": frozenset(["Extra Max HP", "Spell Refresher"]),
        "Repeatable Perks": frozenset(["Extra Life Perk"]),
        "Traps": frozenset(["Trap"]),
        "Wands": frozenset(["Kantele", "Wand (Tier 1)", "Wand (Tier 2)", "Wand (Tier 3)", "Wand (Tier 4)", "Wand (Tier 5)", "Wand (Tier 6)"]),
    }

    def create_regions(self) -> None:
        """Create regions, locations, and connections."""
        create_regions(self.multiworld, self.player)

    def set_rules(self) -> None:
        """Set access rules."""
        set_rules(self)

    def create_items(self) -> None:
        """Create randomized item pool."""
        # First, place any locked items
        self._place_locked_items()

        # Then create the random item pool
        item_pool = []

        for item_name, count in ITEMPOOL_COUNTS.items():
            # Skip event items
            if item_name not in item_table or item_table[item_name].id is None:
                continue

            item_data = item_table[item_name]
            for _ in range(count):
                item = NoitaTestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                item_pool.append(item)

        self.multiworld.itempool += item_pool

    def _place_locked_items(self) -> None:
        """Place items that must be in specific locations (locked placements)."""
        for location_name, item_name in LOCKED_PLACEMENTS.items():
            if item_name and item_name in item_table:
                location = self.multiworld.get_location(location_name, self.player)
                item_data = item_table[item_name]
                item = NoitaTestItem(
                    item_name,
                    item_data.classification,
                    item_data.id,
                    self.player
                )
                location.place_locked_item(item)

    def generate_basic(self) -> None:
        """Place victory event item."""
        victory_location = self.multiworld.get_location("Victory", self.player)

        # Only place if not already filled (e.g., by _place_original_items)
        if victory_location.item is None:
            victory_item = NoitaTestItem(
                "Victory",
                item_table["Victory"].classification,
                None,
                self.player
            )
            victory_location.place_locked_item(victory_item)

        # Set completion condition
        self.multiworld.completion_condition[self.player] = \
            lambda state: state.has("Victory", self.player)

    def create_item(self, name: str) -> Item:
        """Create an item by name."""
        data = item_table[name]
        return NoitaTestItem(name, data.classification, data.id, self.player)

    def fill_slot_data(self) -> Dict[str, object]:
        """Return data for the client."""
        return {}
