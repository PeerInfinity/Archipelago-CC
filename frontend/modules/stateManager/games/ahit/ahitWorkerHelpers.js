import { GameWorkerHelpers } from '../../helpers/gameWorkerHelpers.js';

export class AHITWorkerHelpers extends GameWorkerHelpers {
  constructor(manager) {
    super(manager);
    // console.log('[AHITWorkerHelpers] Initialized.');
  }
  // A Hat in Time-specific worker helper methods will go here.
}
