# Upstream fork-point extraction (git archive from the submodule's objects)
upstream/
# Fork sim extracted from the submodule's committed HEAD (git archive)
fork-head/
# Run outputs
results/
# Full-tree extractions for run-ui-parity.mjs
ui-fork-head/
ui-upstream/
