# Cleanup Backlog

Issues noticed while writing the official procgen documentation (2026-07-01) that are worth fixing but were out of scope for the docs work itself. Each entry has evidence and a suggested fix; none are urgent. Check items off (or delete entries) as they're resolved.

## Code

- [ ] **Duplicated loop-cost logic: `loops/costGenerator.js` ↔ `shared/procgen/loopCostGenerator.js`.** Two parallel implementations of the same algorithm, kept in sync only by a comment ("mirrors the runtime CostGenerator's behavior"). Concretely duplicated: the region-cost formula (`maxMana/2` split across uncosted path regions, floor, min 1), the location-cost formula (`Math.max(1, Math.floor(mana / 2))`), the sphere-log entry extraction (`_extractLocationEntries` vs `extractLocationEntries` — same filter, same items-credited-to-last-location rule, same phantom-entry creation), and the default-fill pass (`_assignDefaultCosts` vs `fillDefaults`). Real drift hazard.
  *Suggested fix:* extract the pure core (entry extraction, both formulas, default fill) into `shared/procgen/loopCostGenerator.js` and have `CostGenerator` import those helpers, keeping only its live-engine orchestration (event dispatch, snapshot waits, loop-state save/restore). Do **not** fold in `loopsCostDebugger/costPlanner.js` — its cost model diverges deliberately (XP levels, per-loop budgets, explore/check phases); at most add a header note there stating the divergence is intentional. `costDataManager.js` is fine as the shared store. Note the submodule boundary: `shared/procgen/` changes need a submodule commit + pointer bump.
- [ ] **Three separate BFS/path implementations in the cost family**: `loopCostGenerator.bfsRegions`, `costPlanner._findPath`, and `costGenerator` delegating to the shared `pathFinder.findPathWithExits`. Minor; worth consolidating only if the item above is done anyway.
- [ ] **`jtaCostDebugger` has its own `JTACostPlanner` "modeled after" `costPlanner.js`** — a third sibling in the planner family, copied rather than shared. Revisit if either planner needs a behavior change.
- [x] **DONE 2026-07-01** (shared submodule `7f16589`: header now defers to the official doc) ~~**`substrateRegistry.js` header contract is incomplete.**~~ The header documents identity/runtime/playback/loopSupport/build-time slots, but real entries carry more: `applyContentModules` (maze), `iframeId` (flash family), `victoryItem`, `zoneCount`/`synthesizeZonePayload`/`extractZoneRules` (zone-based), and bounce's entire sphere-growth adapter surface (`generateZoneForSpecs`, `exitGateVeto`, `buildRegionContract`, `prepareSphereGrowth`, …). The full contract is now documented in `docs/json/developer/procgen/substrate-registry.md`; the header should shrink to the core contract plus a pointer to that doc rather than trying (and failing) to be exhaustive. Submodule caveat applies.
- [ ] **`gridGrowth` is not marked deprecated in the pipeline panel.** The Mode toggle (`frontend/modules/procgenPipeline/procgenPipelineUI.js:653`) presents "Grid growth (build from a scenario pool)" as a peer of the others. Since it's deprecated in favor of sphere growth, the label should say so — docs alone won't stop someone selecting it.
- [ ] **`jta` `zoneCount: 16` is hand-synced** with the zone table in `iframe_games/journey-to-ascension/build/zones.js` (acknowledged in the entry's comment; drift is only caught by a runtime warning on `loadZone`). Consider generating or asserting it at build/test time.

## Documentation / repo hygiene

- [ ] **`?mode=loops` is not a real mode — decide its fate.** `frontend/modes.json` has no `loops` mode; the parameter works via a special case in `frontend/modules/loops/loopUI.js:956` that reads `mode === 'loops'` and auto-enables loop mode (backward compatibility for the old URL). The mode system itself falls back to the default layout. Confusing double meaning of `mode`. Options (user, 2026-07-01): (a) remove the special case entirely and update the README demo link, or (b) move the special case to a dedicated parameter (e.g. `?loops=1`), optionally keeping `mode=loops` as a deprecated alias for a while. The `?metagame=…` README links are still unverified.

- [x] **DONE 2026-07-01** — all ~90 NewDocs references repointed or removed (incl. both submodules); the final sweep grep returns nothing. Record of what replaced what: [newdocs-reference-map.md](./newdocs-reference-map.md).
- [ ] **`rule_builder_modified/` and `rule_builder_original/` sit at the repo root** with their own READMEs — diff artifacts that pollute the README namespace and can be mistaken for the real `rule_builder/`. Check whether `docs/json/developer/diffs/rule-builder/` references them; either move them under the diffs area or delete them if the diff docs stand alone.
- [x] **DONE 2026-07-01** (`f8cd8d857`) — module doc index now covers all 79 directories, with procgen/loops/game-tool sections and disabled-by-default markers.
- [x] **DONE 2026-07-01** (`ce335e718`) — flagship READMEs (root, frontend, exporter, world_generator) now cover procgen and the round-trip; frontend's stale module count fixed.

## Deferred feature follow-ups (from completed projects)

Recorded here when the originating project wrapped so they aren't lost; none are urgent.

- [ ] **Per-sphere batching follow-ups**: zone-substrate bare-rules reconstruction in `rebuildEnvelopeFromRulesJson`, and a CLI-only truncate-to-wave operation.
- [ ] **Loops rework deferrals**: jta custom-queue recording (`loopSupport.customQueues` flip once recording exists), and bounce native per-tick mana drain (loops currently charges on action completion).
- [ ] **`xpEffect` `'speed'` / `'both'` variants** — reserved in the loop_costs schema, deferred pending tick-speed regulation.
- [ ] **Maze content modules phase 3 (block-pushing)** — planned but never started; the `onMove` hook and queue verb are already in place.
- [ ] **Rename gameState `maxMana` → `startingMana`** — per the 2026-07-05 JtA substrate rulings, "max mana" is really the loop's STARTING mana (what a loop reset refills to); it is no longer a ceiling (gains/`_recomputeMaxMana` no longer clamp). The name touches gameState's API surface (`getMaxMana`, `setSubstrateMaxManaBonus`, `manaChanged` payloads, `includePerItemMaxMana`), loops/substrate UIs, and docs — user judged the rename possibly worth the churn; do it as one dedicated pass.
- [x] **DONE 2026-07-06** — **JSON Tools on compiled (frozen) AP — world-shaped components as custom apworlds.** `demo_worlds` and `tracker` are now packed as source-bearing `.apworld` files into `custom_worlds/` on frozen installs (`Component.frozen_apworld`); `worldgen_worlds` and the `toem_rule_builder` demo world are skipped with a warning (extended rule_builder can't load frozen). Verified on the frozen 0.6.7 bench: real archive download + extraction inside the frozen exe, world types 85→90, zero load failures.
- [ ] **If frozen romless/world-patching is ever attempted** (repacking bundled apworld zips with patched files): it would break the `world_source` exporter fallback's correctness invariant — the fallback trusts that frozen code is the *pristine* upstream release and extracts rule text by line number from the matching tag's source. Patched `.pyc` + pristine `.py` = silently wrong extracted rules. The patch step would have to overlay its patched `.py` files into `json_tools_world_source/<version>/` as part of applying. See `worlds/json_tools_installer/installer/world_source.py` docstring and the project memory file.
- [ ] **scan-build workflow (Native Code Static Analysis) failing since ~2026-06-26**: the analysis itself passes ("No bugs found"), but the `setup.py build -y` step dies in cx_Freeze's ModuleFinder on `ImportError: No module named 'worlds.ut_pickle.TrackerClient'` while walking `worlds` subpackages. Pre-dates the 2026-07-07 requirements change that woke the workflow (it only triggers on `.c/.h/setup.py/requirements.txt` paths; last green 2026-03-21). Likely fix: exclude `ut_pickle` from the frozen build's package walk or make its client modules import-safe for cx_Freeze.

## Tooling notes (Claude Code sessions)

- **Plain `grep` silently returns nothing on some frontend sources** (files containing emoji/multibyte UTF-8, e.g. `procgenPipelineUI.js`) — the match exists but grep produces no output and no error. Prefix with `LC_ALL=C` (`LC_ALL=C grep -n pattern file`) or use the Grep tool. This has repeatedly cost debugging time; if it keeps biting, consider adding it to CLAUDE.md's gotchas table.
