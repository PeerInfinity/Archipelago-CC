# Cleanup Backlog

Issues noticed while writing the official procgen documentation (2026-07-01) that are worth fixing but were out of scope for the docs work itself. Each entry has evidence and a suggested fix; none are urgent. Check items off (or delete entries) as they're resolved.

## Code

- [ ] **Duplicated loop-cost logic: `loops/costGenerator.js` ↔ `shared/procgen/loopCostGenerator.js`.** Two parallel implementations of the same algorithm, kept in sync only by a comment ("mirrors the runtime CostGenerator's behavior"). Concretely duplicated: the region-cost formula (`maxMana/2` split across uncosted path regions, floor, min 1), the location-cost formula (`Math.max(1, Math.floor(mana / 2))`), the sphere-log entry extraction (`_extractLocationEntries` vs `extractLocationEntries` — same filter, same items-credited-to-last-location rule, same phantom-entry creation), and the default-fill pass (`_assignDefaultCosts` vs `fillDefaults`). Real drift hazard.
  *Suggested fix:* extract the pure core (entry extraction, both formulas, default fill) into `shared/procgen/loopCostGenerator.js` and have `CostGenerator` import those helpers, keeping only its live-engine orchestration (event dispatch, snapshot waits, loop-state save/restore). Do **not** fold in `loopsCostDebugger/costPlanner.js` — its cost model diverges deliberately (XP levels, per-loop budgets, explore/check phases); at most add a header note there stating the divergence is intentional. `costDataManager.js` is fine as the shared store. Note the submodule boundary: `shared/procgen/` changes need a submodule commit + pointer bump.
- [ ] **Three separate BFS/path implementations in the cost family**: `loopCostGenerator.bfsRegions`, `costPlanner._findPath`, and `costGenerator` delegating to the shared `pathFinder.findPathWithExits`. Minor; worth consolidating only if the item above is done anyway.
- [ ] **`jtaCostDebugger` has its own `JTACostPlanner` "modeled after" `costPlanner.js`** — a third sibling in the planner family, copied rather than shared. Revisit if either planner needs a behavior change.
- [x] **DONE 2026-07-01** (shared submodule `7f16589`: header now defers to the official doc) ~~**`substrateRegistry.js` header contract is incomplete.**~~ The header documents identity/runtime/playback/loopSupport/build-time slots, but real entries carry more: `applyContentModules` (maze), `iframeId` (flash family), `victoryItem`, `zoneCount`/`synthesizeZonePayload`/`extractZoneRules` (zone-based), and bounce's entire sphere-growth adapter surface (`generateZoneForSpecs`, `exitGateVeto`, `buildRegionContract`, `prepareSphereGrowth`, …). The full contract is now documented in `docs/json/developer/procgen/substrate-registry.md`; the header should shrink to the core contract plus a pointer to that doc rather than trying (and failing) to be exhaustive. Submodule caveat applies.
- [ ] **`gridGrowth` is not marked deprecated in the pipeline panel.** The Mode toggle (`frontend/modules/procgenPipeline/procgenPipelineUI.js:653`) presents "Grid growth (build from a scenario pool)" as a peer of the others. Since it's deprecated in favor of sphere growth, the label should say so — docs alone won't stop someone selecting it.
- [ ] **`jta` `zoneCount: 16` is hand-synced** with the zone table in `iframe_games/journey-to-ascension/build/zones.js` (acknowledged in the entry's comment; drift is only caught by a runtime warning on `loadZone`). Consider generating or asserting it at build/test time.

## Documentation / repo hygiene

- [ ] **`?mode=loops` is not a real mode — decide its fate.** `frontend/modes.json` has no `loops` mode; the parameter works via a special case in `frontend/modules/loops/loopUI.js:956` that reads `mode === 'loops'` and auto-enables loop mode (backward compatibility for the old URL). The mode system itself falls back to the default layout. Confusing double meaning of `mode`. Options (user, 2026-07-01): (a) remove the special case entirely and update the README demo link, or (b) move the special case to a dedicated parameter (e.g. `?loops=1`), optionally keeping `mode=loops` as a deprecated alias for a while. The `?metagame=…` README links are still unverified.

- [x] **DONE 2026-07-01** — all ~90 NewDocs references repointed or removed (incl. both submodules); the final sweep grep returns nothing. Record of what replaced what: [newdocs-reference-map.md](./newdocs-reference-map.md).
- [ ] **`rule_builder_modified/` and `rule_builder_original/` sit at the repo root** with their own READMEs — diff artifacts that pollute the README namespace and can be mistaken for the real `rule_builder/`. Check whether `docs/json/developer/diffs/rule-builder/` references them; either move them under the diffs area or delete them if the diff docs stand alone.
- [x] **DONE 2026-07-01** (`f8cd8d857`) — module doc index now covers all 79 directories, with procgen/loops/game-tool sections and disabled-by-default markers.
- [x] **DONE 2026-07-01** (`ce335e718`) — flagship READMEs (root, frontend, exporter, world_generator) now cover procgen and the round-trip; frontend's stale module count fixed.

## Deferred feature follow-ups (from completed projects)

Recorded here when the originating project wrapped so they aren't lost; none are urgent.

- [ ] **Per-sphere batching follow-ups**: zone-substrate bare-rules reconstruction in `rebuildEnvelopeFromRulesJson`, and a CLI-only truncate-to-wave operation.
- [ ] **Loops rework deferrals**: jta custom-queue recording (`loopSupport.customQueues` flip once recording exists), and bounce native per-tick mana drain (loops currently charges on action completion).
- [ ] **`xpEffect` `'speed'` / `'both'` variants** — reserved in the loop_costs schema, deferred pending tick-speed regulation.
- [ ] **Maze content modules phase 3 (block-pushing)** — planned but never started; the `onMove` hook and queue verb are already in place.
- [ ] **Rename gameState `maxMana` → `startingMana`? — RECONSIDERED, likely NOT worth it (user 2026-07-08).** Per the 2026-07-05 JtA substrate rulings, "max mana" is really the loop's STARTING mana (what a loop reset refills to) and is no longer a hard ceiling (gains/`_recomputeMaxMana` no longer clamp). BUT `maxMana` also *is* the value rendered as the mana-bar's max — current mana may legitimately exceed it, so "max" is still meaningful as "the displayed max," just not a cap. Given that dual role, the user is leaning toward **keeping `maxMana`** rather than renaming. If ever renamed, it touches gameState's API surface (`getMaxMana`, `setSubstrateMaxManaBonus`, `manaChanged` payloads, `includePerItemMaxMana`), loops/substrate UIs, and docs — one dedicated pass. **Same reconsideration applies to the JtA `max_energy` terminology:** in JtA "max energy" is likewise *starting* energy that current energy can exceed by design — but per the scope rule below it's original JtA code and stays regardless.
  - **SCOPE — rename fork-introduced names only.** Per user (2026-07-08): do NOT rename any variable that came from the original JtA code (e.g. `GAMESTATE.max_energy` / `current_energy` in the `journey-to-ascension` submodule, or the `maxEnergy`/`currentEnergy` fields in its substrate-hook callback payloads that mirror them). Do NOT rename the **perks** either (e.g. keep `"special": "max_energy_50"` in `worlds/jta/jta_game_data.json` as-is). Only rename names WE introduced in the fork/host (gameState `maxMana` → `startingMana` and its API surface; fork-only harness knobs like `pinMaxEnergy` if desired).
  - **CAP AUDIT + comment/harness cleanup — DONE 2026-07-08.** No max-energy *ceiling* exists in the JtA engine (confirmed: `modifyEnergy` does a bare `current_energy += delta`, and current energy exceeding max is *intended* JtA behavior). Actions taken: (1) fixed the two **stale comments** that wrongly said gains were "clamped to maxMana" (bridge.js gains-mirror comment + index.js `BRIDGE_GAIN_MANA` comment) — they now state maxMana is the STARTING mana / bar-max, not a ceiling; (2) the stats-harness `pinMaxEnergy` "clamp" (`CC/scripts/jta-stats/driver.mjs`) turned out NOT to be an erroneous ceiling — it's the **loop-reset refill** to the pinned starting energy (applied only at init + each reset/prestige boundary, never mid-loop), matching the host's `refillMana` discarding surplus. Rewrote it from `if (current > pin) current = pin` to an explicit `current = pin` reset-refill with an accurate comment; verified behavior-neutral via before/after run of `pin100-defaults` (2000 runs / 57692 ticks / 130-134 tasks byte-identical apart from wall-clock timing).
  - **PRODUCER LEG — DONE + ENABLED 2026-07-08.** `moduleSettings.jtaSubstrateWrapper.energyBonusSync` now **defaults ON** (user ruling 2026-07-08: bonus-sync is the intended runtime, superseding the 2026-07-05 pinned default — substrate play is natively standalone-paced, the pacing target zone-rando anchors on). The "re-measurement before enabling" caveat was satisfied by *existing* data: profile-vanilla's standalone variant (≡ bonus-sync for JtA-only) completes 264/269 tasks in 3000 runs vs pinned 203/269 — standalone tuned-defaults progress cleanly. Companion setting `moduleSettings.gameState.includePerItemMaxMana` (default on) gates the AP per-item term so it doesn't double-count against JtA energy. Design (`NewDocs/plans/jta/jta-substrate-v1-plan.md:192-205`): `maxMana = sharedDefault + Σ(per-substrate bonuses)`; JtA's own starting-energy bonuses feed its bonus via `setSubstrateMaxManaBonus('jta', …)`. Implemented across the fork + host + bridge:
    - Fork (submodule `31447f7`): `GAMESTATE.jta_starting_energy_bonus` accumulator mirrors every starting-energy-bonus write to `max_energy` (Energetic Memory, EnergySpell perk, Divine Supremacy, Energized — the four sources the user confirmed 2026-07-08), resets to 0 with `max_energy`, exposed on `getFullState`. Deliberately NOT touched by the host pin, so it reports JtA's *native* bonus regardless of what `max_energy` is pinned to (avoids the pin↔bonus feedback loop). Verified passive (pin100 harness byte-identical) and == `max_energy-100` at every boundary (400/400).
    - Host (`index.js`): `energyBonusSync` setting + schema; handles `jta:bridgeSetManaBonus` → `setSubstrateMaxManaBonus('jta', bonus)`; zeroes the contribution when the flag is off; pushes the flag to the bridge via `initialState`.
    - Bridge (`bridge.js`): reads the flag; when on, `_pollTick` reports `jtaStartingEnergyBonus` changes up and `_syncEnergyFromPool` syncs CURRENT energy only (JtA owns `max_energy` — no pin). When off, the legacy pin is unchanged.
    - Test: `jta-starting-energy-bonus-raises-pool` in the substrates config drives the real path end-to-end (17/17 green).
    - **Balance note (accepted, now the default):** with the flag on, JtA max-energy growth raises the pool AND re-enables Divine-Speed `overflow` scaling (`simulation.ts:3937`, inert while pinned) — both intended (user confirmed 2026-07-08). No separate harness emulation was needed: standalone ≡ bonus-sync for JtA-only, so the existing `pinMaxEnergy: null` variant already models the runtime; `pinned100` stays for comparison.
  - Terminology touch points (only relevant if the rename is ever revived): `CC/scripts/jta-stats/` (`pinMaxEnergy` in the README/configs, `maxEnergy` fields in `results/vanilla-profile.json`, SUMMARY.md's description). (The unrelated `max_energy`/`maxEnergy` in factorio, sc2, sm/variaRandomizer, stardew_valley are upstream world code — leave them alone.)
- [x] **DONE 2026-07-06** — **JSON Tools on compiled (frozen) AP — world-shaped components as custom apworlds.** `demo_worlds` and `tracker` are now packed as source-bearing `.apworld` files into `custom_worlds/` on frozen installs (`Component.frozen_apworld`); `worldgen_worlds` and the `toem_rule_builder` demo world are skipped with a warning (extended rule_builder can't load frozen). Verified on the frozen 0.6.7 bench: real archive download + extraction inside the frozen exe, world types 85→90, zero load failures.
- [ ] **If frozen romless/world-patching is ever attempted** (repacking bundled apworld zips with patched files): it would break the `world_source` exporter fallback's correctness invariant — the fallback trusts that frozen code is the *pristine* upstream release and extracts rule text by line number from the matching tag's source. Patched `.pyc` + pristine `.py` = silently wrong extracted rules. The patch step would have to overlay its patched `.py` files into `json_tools_world_source/<version>/` as part of applying. See `worlds/json_tools_installer/installer/world_source.py` docstring and the project memory file.
- [x] **DONE 2026-07-07** — **scan-build workflow (Native Code Static Analysis) failing since ~2026-06-26**: root cause was PEP 701 f-string quote reuse (`f"...{get_ut_color("x")}..."`) in `worlds/ut_pickle/TrackerClient.py` — valid on Python 3.12+, SyntaxError on the workflow's Python 3.11; cx_Freeze's ModuleFinder reported the parse failure as `ImportError: No module named 'worlds.ut_pickle.TrackerClient'`. Fixed by swapping inner quotes (`3d11010ef`); scan-build green again. NOTE: the three `scripts/test/fixtures/tracker_original*/TrackerClient.py` copies retain the 3.12-only syntax deliberately (pristine upstream-UT diffing fixtures; not frozen, not imported) — upstream Universal Tracker itself won't run its client on 3.11. scan-build now also supports `workflow_dispatch`.
- [ ] **Small generated worlds fail fill on some random seeds** (was filed as "Runner WorldGen fails fill") — `Fill.FillError: No more spots to place N items. Remaining locations are invalid.` in `test/general/test_ids.py::test_postgen_datapackage`, `test_implemented.py::test_explicit_indirect_conditions_spheres`, and `test/multiworld` two-player fills. Seed-dependent: unittests matrix jobs pass or fail depending on their random seed (observed 2026-07-07 on both pre-session and post-session commits — NOT caused by the vendored-_ext regeneration; canonical seed 1 generates fine). **Broader than runner_worldgen (2026-07-09): the same failure hit `game='APCalc'` (seed 14770670700670094370, "9 items") on macOS-3.13 while every other job passed, and the whole `unittests` workflow was fully green on the next push.** So the failing job/game/seed MOVES between runs — a changed game name is not a new bug. Likely cause is generic: worlds with few items and few locations, where fill paints itself into a corner on unlucky seeds. Related: `project_runner_substrate`, `project_ci_preexisting_red` memory topics.
- [ ] **`runnerDemo` vitest suites are TEMPORARILY disabled** (2026-07-09, user request, `d34548cf3`) — excluded from both `vitest.config.js` and `vitest.slow.config.js`. Reason: the serial slow job pushed the `JavaScript Unit Tests` workflow to ~21 min; without them it is ~2 min, and the intermittent local `Test timed out in 10000ms` flake in the default run disappeared. Decide whether to re-enable (delete the `runnerDemo` entry from both configs), split them into a separate scheduled workflow, or make the exclusion permanent. Note `frontend/modules/procgenPipeline/runnerSphereGrowth.slow.test.js` is runner-driven but lives outside `runnerDemo/`, so it still runs (7 tests, ~52 s) and is currently the ONLY runner coverage; the bounce equivalents are `braidSphereBot.slow.test.js` / `topDownBounce.slow.test.js`. GOTCHA: `vitest.slow.config.js` had no `exclude` key, so any exclude added there must spread `configDefaults.exclude` or `node_modules`/`dist` stop being ignored.

- [ ] **jtaBalance walk: a task can complete before its solve fires and keeps its Pass-A provisional cost** (found 2026-07-10 by the Phase-5e §4.2 measurement pass; ≤1 task per affected world — e.g. dataset worlds ds1-f1 task 195, ds3-f3 task 193). Mechanism: the walk releases an entry, the sim completes it organically within ONE run, and the run-boundary fallback only solves entries still pending after ≥2 runs — so `setTaskFirstStartCallback` never assigns a cost. Coverage is unaffected (the task completed) and the report now counts it separately as `completedUnsolved` (the convergence bar no longer fails on it), but the task is REPLAYED every subsequent run at its provisional multiplier, so the replay economy carries one unsolved cost. Candidate fix: in the walk's completion callback, if the frontier entry is still in `pendingEntry`, run `solveEntry(id, k, 'completion')` before advancing. Deliberately NOT done as a drive-by (2026-07-10): it changes solve output for affected worlds, which invalidates localStorage patch caches (key is still `_v1_`) and the committed `results/dataset-passb/` measurement records — land it as its own step with a batch re-run. Open question while there: why first-start doesn't fire for these entries at all (suspected: the entry starts inside the same boundary window it was released in, before a "fresh" start the callback recognizes).

- [ ] **Port the jtaActionQueue module to the substrate JtA** (user request 2026-07-10). The old March-stack `jtaActionQueue`/`jtaQueueEngine` drive their own `jta-remote/game-bundle` copy in `?mode=jta` and do not work against the `journey-to-ascension` submodule. The substrate arc deliberately chose a different strategy (extend the game's own automation), and that decision stands; this port is wanted **even if the module remains unused**, so the queue-UI capability isn't lost when the old stack is retired. Scope: make jtaActionQueue talk to the substrate wrapper/bridge (or the fork's window hooks) instead of the jta-remote client. Feeds the Phase-6 absorption audit: one of the "features to absorb before deleting the old stack."

- [x] **DONE 2026-07-10 (user chose regenerate)** — all 35 non-legacy `spark-off-*` result files + the seven `comparison-spark-off-*.md` reports regenerated on the Fork 1.7 build; SUMMARY.md Round 9 records the regeneration, the two-knob provenance (stall default 40→20 AND `baselineMods()` savings-ON — the 8 files pinning both knobs reproduced gameplay-byte-identically, the other 27 changed exactly as predicted), and that Round 6's shipped conclusions are unchanged on the fresh numbers. Historical-era files remain in git history; LEGACY spark-on-era files (`baseline-node.json` etc.) stay as documented historical artifacts. ~~**jta-stats committed z15 baseline predates the Fork 1.6.1 stall default**~~ (found 2026-07-10 during Phase 5c; RESOLVED same day). `results/spark-off-z15-baseline-node.json` (2026-07-06 23:40, 261 runs) no longer reproduces: today's builds give **237 runs / 9173 ticks**. Root cause: `baselineMods()` does not set `auto_prestige_stall_resets`, so the run inherits the GAME default — which Fork 1.6.1 changed 40→20 later that same evening. Proof: the committed `spark-off-z15-stall-20-node.json` (23:54 the same day) is **exactly 237 runs / 9173 ticks**, matching today's out-of-the-box run tick-for-tick (also showing Unlock Savings is a no-op on this z15 trajectory — identical numbers with savings on or off, consistent with Round 6's "z15 secondary insensitive"). No engine or harness drift: Fork 1.6.2 and Fork 1.7 builds and the pre/post-5c driver all reproduce 237/9173. Remaining decision only: regenerate the committed spark-off result files on the current build (breaking continuity with SUMMARY.md's recorded numbers) or leave them as clearly-dated historical artifacts with this note. Any future comparison against a committed spark-off file should check whether its mods predate the 1.6.1 stall default.

- [x] **DONE 2026-07-09** (`87197f704` bridge + `perkOrigin.js`, `4441a99d8` in-app coverage) ~~**JtA bridge: AP perk grants do not survive a prestige**~~ (found 2026-07-09 by Phase-4 emergent verification; measured, not theoretical). `doPrestige()` set every perk to `false`; local perk grants are suppressed (`task_patches` set `perk` → Count) and each AP item is received once, so a prestige cost the player every perk permanently. Reachable inside v1 scope: task 153 "Touch the Divine" is a `TaskType.Prestige` task in **zone 14**. Cost, measured with `randomized-pacing-no-regrant` (which modelled the shipped bridge exactly): 2/130, 3/130 and 5/130 AP locations **never checked** on seeds 1, 2 and 4 (seed 3 never prestiges); nine of the ten stranded were `thresholdFloored` tasks. **Fixed to the ruled semantics:** an own-world perk is granted on EVERY full completion of the task holding it, so a prestige wipes it and the next completion brings it back (its AP location stays checked); a foreign perk has no task to re-run, so the bridge grants it on arrival and re-grants it after a prestige, detected by comparing `getFullState().prestigeCount` across the energy-reset callback. Origin comes from `staticData.locationItems` + `playerId` (post-fill placement, `perkOrigin.js`), NOT from the region sidecar — sidecars are baked at world-generation time and read back verbatim at export, while AP's fill runs afterwards, so a sidecar placement map would be pre-fill and wrong for any `Generate.py` world. `adapterClient` item origin was the other candidate and is unavailable: the frontend normally runs with no AP server, so no `ReceivedItems` arrives. No fork changes. In-app coverage: `jta-prestige-perk-regrant` (new `jta_prestige_test` preset — a `start_inventory` perk is the foreign fixture, since solo v1 worlds place all 21 perks at home), plus 6 unit tests on the classifier. Note the sweep cannot verify this: `sweep-ap-seeds.mjs` drives `driver.mjs`, whose `baseline` column already models the intended semantics. See `docs/json/developer/procgen/jta.md` and SUMMARY Round 8.

## Tooling notes (Claude Code sessions)

- **Plain `grep` silently returns nothing on some frontend sources** (files containing emoji/multibyte UTF-8, e.g. `procgenPipelineUI.js`) — the match exists but grep produces no output and no error. Prefix with `LC_ALL=C` (`LC_ALL=C grep -n pattern file`) or use the Grep tool. This has repeatedly cost debugging time; if it keeps biting, consider adding it to CLAUDE.md's gotchas table.
