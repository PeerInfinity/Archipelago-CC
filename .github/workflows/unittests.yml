# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: unittests

on:
  push:
    paths:
      - '**'
      - '!docs/**'
      - '!deploy/**'
      - '!setup.py'
      - '!Dockerfile'
      - '!*.iss'
      - '!.gitignore'
      - '!.dockerignore'
      - '!.github/workflows/**'
      - '.github/workflows/unittests.yml'
  pull_request:
    paths:
      - '**'
      - '!docs/**'
      - '!deploy/**'
      - '!setup.py'
      - '!Dockerfile'
      - '!*.iss'
      - '!.gitignore'
      - '!.dockerignore'
      - '!.github/workflows/**'
      - '.github/workflows/unittests.yml'

permissions: {}

jobs:
  unit:
    runs-on: ${{ matrix.os }}
    name: Test Python ${{ matrix.python.version }} ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python:
          - {version: '3.11.2'} # Change to '3.11' around 2026-06-10
          - {version: '3.12'}
          - {version: '3.13'}
        include:
          - python: {version: '3.11'}  # old compat
            os: windows-latest
          - python: {version: '3.13'}  # current
            os: windows-latest
          - python: {version: '3.13'}  # current
            os: macos-latest

    steps:
    - uses: actions/checkout@v6.0.2
    - name: Set up Python ${{ matrix.python.version }}
      uses: actions/setup-python@v6.2.0
      with:
        python-version: ${{ matrix.python.version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r ci-requirements.txt
        python ModuleUpdate.py --yes --force --append "WebHostLib/requirements.txt"
        # Launcher.py:28 calls ModuleUpdate.update() with NO arguments, i.e.
        # yes=False, so any requirement the line above failed to install makes
        # it call input() — on a runner with no stdin that is an EOFError and a
        # red job. The line above is the real installer; this second check is
        # redundant in CI, so skip it. Do NOT hoist this to job/workflow env:
        # it would also disable the installer above.
        SKIP_REQUIREMENTS_UPDATE=1 python Launcher.py --update_settings  # make sure host.yaml exists for tests
    - name: Unittests
      env:
        # Force UTF-8 stdio. Without this, Python 3.11 on Windows defaults
        # sys.stdout to a cp1252 TextIOWrapper, and pytest-xdist's parent-
        # worker stdout teardown trips a "ValueError: underlying buffer has
        # been detached" inside _pytest.config.console_main's final
        # sys.stdout.flush() — failing the job after every test passed.
        # Python 3.13 uses UTF-8 by default and is unaffected.
        PYTHONUTF8: "1"
        PYTHONIOENCODING: "utf-8"
      run: |
        pytest -n auto

  hosting:
    runs-on: ${{ matrix.os }}
    name: Test hosting with ${{ matrix.python.version }} on ${{ matrix.os }}

    strategy:
      matrix:
        os:
          - ubuntu-latest
        python:
          - {version: '3.13'}  # current

    steps:
    - uses: actions/checkout@v6.0.2
    - name: Set up Python ${{ matrix.python.version }}
      uses: actions/setup-python@v6.2.0
      with:
        python-version: ${{ matrix.python.version }}
    - name: Install dependencies
      run: |
        python -m venv venv
        source venv/bin/activate
        python -m pip install --upgrade pip
        python ModuleUpdate.py --yes --force --append "WebHostLib/requirements.txt"
    - name: Test hosting
      env:
        # This test spawns Generate.py / MultiServer.py as SUBPROCESSES, and
        # each runs ModuleUpdate.update() at __main__ with yes=False. A
        # dependency that failed to install therefore turns into an input()
        # call with no stdin — EOFError, red job, and a message that names the
        # prompt rather than the install failure. Safe here: the install step
        # above already ran ModuleUpdate --yes --force.
        SKIP_REQUIREMENTS_UPDATE: "1"
      run: |
        source venv/bin/activate
        export PYTHONPATH=$(pwd)
        timeout 600 python test/hosting/__main__.py
